# v3.1 template diet — literal-preservation table

Derived from `scripts/verify-template-consistency.sh` (checks numbered 1-34 as
of this commit, HEAD `5d7bbb1`) and `hooks/require-skills-block.sh`. Every row
below was re-grepped against the current file — line numbers are what the grep
returns today, not copied from the task-0 brief (which is stale). This table is
the input every later diet task must consult before cutting a line: if a cut
would remove one of these literals/regexes/markers, the cut needs a companion
edit to the check (or this row is wrong and should be fixed here first).

## Method

```bash
grep -nE 'templates/\*/(CLAUDE|AGENT_TEAM|CLAUDE\.local)\.md|templates/general/(CLAUDE|AGENT_TEAM|CLAUDE\.local)\.md' scripts/verify-template-consistency.sh
grep -nE 'AGENT_TEAM|CLAUDE\.md|CLAUDE\.local' hooks/*.sh
```

plus targeted reads of checks 2, 3, 4, 5, 6, 8, 9, 10, 12, 20, 25, 26, 30, 31,
34, of `hooks/require-skills-block.sh` in full, and a direct grep for
`CLAUDE\.local\.md` across `scripts/verify-template-consistency.sh` and
`hooks/*.sh`.

## Table

| file | literal / regex | required by |
|---|---|---|
| `templates/*/CLAUDE.md` ×6 | literal `context-mode` (any occurrence) | Two consumers. (1) The context-mode MCP server's `writeRoutingInstructions()` in `~/.claude/plugins/marketplaces/context-mode/server.bundle.mjs` (measured 2026-09-05): `return a.includes("context-mode")?null:(...)` — if the file does NOT contain the string, the server appends its own routing block on next run. Confirmed by direct grep of the bundle; the same `.includes("context-mode")` predicate recurs for the Gemini/OpenCode variants of the same writer. (2) check 26, `verify-template-consistency.sh:1837-1858`, which requires the occurrence on the SPECIFIC line immediately above `<!-- PROJECT-CUSTOM:BEGIN -->` in each variant `CLAUDE.md` (currently `templates/general/CLAUDE.md:154`, one line above the marker at `:155`) — "anywhere in the file" is necessary for consumer (1) but not sufficient for consumer (2); check 26 also pins the literal's *position*. |
| `templates/*/CLAUDE.md` ×6 | `superpowers:` (any occurrence) | check 2, `verify-template-consistency.sh:27` (`grep -q "superpowers:" "templates/$v/CLAUDE.md"`). Verified: 12 hits total across `templates/*/{CLAUDE,AGENT_TEAM}.md`. |
| `templates/*/CLAUDE.md` ×6 | `^## Superpowers Skills — MUST Invoke Before Responding$` | check 3, `verify-template-consistency.sh:48` |
| `templates/*/{AGENT_TEAM,CLAUDE}.md` ×6 each | absence-only: `Plan Challenge Protocol\|EnterPlanMode\|Challenge 1\|Challenge 2\|tier-before-coder` (a match FAILS the check) | check 6, `verify-template-consistency.sh:89,94` (`GATE_LITERALS=...`; `grep -qE "$GATE_LITERALS" "$f"` triggers `ko`). Note: `hooks/tier-before-coder.sh` no longer exists (`ls hooks/` verified 2026-09-05 — removed in the v2.1 PR7 plan-gate deletion this check's own comment at `:83-88` documents), so there is no "grep the hook for plan-template lines" row to add — the literal must stay absent from prose, full stop. |
| `templates/*/CLAUDE.md` ×6 | `karpathy-guidelines` | check 20, `verify-template-consistency.sh:721` (`grep -q 'karpathy-guidelines' "templates/$v/CLAUDE.md"`) — the pointer from CLAUDE.md to the skill that now owns the 11 developer-agent preferences. Verified: 6 hits, one per variant. |
| `templates/*/CLAUDE.md` ×6 | `<!-- PROJECT-CUSTOM:BEGIN -->` … `<!-- PROJECT-CUSTOM:END -->` markers, verbatim | check 26, `verify-template-consistency.sh:1837-1858`, position-sensitive per the context-mode row above. Survive until the ownership commit, which removes them, `region.sh` (the sync-template extractor) and the region checks (25, 26, 31) in the same commit — do not strip the markers piecemeal while any of those are still live. |
| `templates/*/AGENT_TEAM.md` and `templates/*/.claude/agents/*.md` (region-bearing set) | `<!-- PROJECT-CUSTOM:BEGIN -->` present, `<!-- PROJECT-CUSTOM:END -->` as the file's literal LAST LINE | check 25, `verify-template-consistency.sh:1811-1828` |
| `templates/general/AGENT_TEAM.md` (byte-identical ×6) | `superpowers:` (any occurrence) | check 2, `verify-template-consistency.sh:32` |
| `templates/*/AGENT_TEAM.md` | `^### Spawn-Prompt Binding Table$` | check 4, `verify-template-consistency.sh:64` |
| `templates/*/AGENT_TEAM.md` | `Spawn-prompt skill injection` | check 5, `verify-template-consistency.sh:75` |
| `templates/*/AGENT_TEAM.md` | absence-only: same `GATE_LITERALS` as the CLAUDE.md row above | check 6, `verify-template-consistency.sh:89,94` (the loop tests both files per variant) |
| `templates/general/AGENT_TEAM.md` | count of `^## Required Skills$` lines, floor 5 | check 8, `verify-template-consistency.sh:116` (`grep -c "^## Required Skills$" ...`; `-ge 5`). Verified: current count is exactly 5 — AT the floor, zero slack for the diet to cut one. |
| `templates/general/AGENT_TEAM.md` | row `^\| \`coder\`` must NOT contain `requesting-code-review`; row `^\| \`architect\`` must contain `brainstorming` | check 9, `verify-template-consistency.sh:126-144` |
| `templates/general/AGENT_TEAM.md` + `hooks/require-skills-block.sh` | every `(subagent, skill)` pair below present in BOTH files (a skill absent from both sides passes vacuously — the check's own warning, `:171-179`) | check 10, `verify-template-consistency.sh:150-192` (`check_pair` calls at `:166-182`), cross-checked against the `case` arms in `hooks/require-skills-block.sh:99-152`. Pairs: `coder`/`karpathy-guidelines`, `coder`/`test-driven-development`, `coder`/`verification-before-completion`, `coder`/`receiving-code-review`, `tester`/`systematic-debugging`, `tester`/`test-driven-development`, `architect`/`writing-plans`, `architect`/`brainstorming`. Plus an absence assertion: `hooks/require-skills-block.sh` must NOT contain `requesting-code-review` (`:185-188`). |
| `templates/*/AGENT_TEAM.md` and `templates/*/CLAUDE.md` | absence-only regex `banned='fixes code directly\|PO only\|PO fixes directly\|PO reviews directly\|PO verifies directly\|may be committed directly'` | check 12, `verify-template-consistency.sh:296-303` (`grep -rEl "$banned" templates/*/AGENT_TEAM.md templates/*/CLAUDE.md`) — a second absence assertion over exactly the two files the diet cuts; missed by the Step-1 grep because it names the files via a shell glob, not a per-variant loop. |
| every tracked `.md` (except `CHANGELOG.md`) and `hooks/*.sh`, including the three diet files | heading citations of the shape `` file.md -> Heading ``, `file.md -> "Heading"`, `` file.md -> *Heading* ``, `` file.md § Heading `` must resolve to a real `#`-heading in the named file | check 30, `verify-template-consistency.sh:2453-2609`. This is broader than any single heading: it is a live mechanism, not a fixed list — any heading in `templates/*/CLAUDE.md` or `templates/*/AGENT_TEAM.md` that is cited by name from ANYWHERE in the repo (including hook error messages, e.g. `hooks/require-skills-block.sh:176` cites `` AGENT_TEAM.md §Spawn-Prompt Binding Table ``) breaks arm A if renamed without updating every citation. Arm C floors the collected-citation count at 40 (currently well above), so a diet that renames headings must re-run this check (or `grep -rnoE` the same four shapes) rather than eyeball it. Do not rename `## Superpowers Skills — MUST Invoke Before Responding`, `### Spawn-Prompt Binding Table`, or any heading named in this table without a repo-wide citation sweep first. |
| `templates/general/AGENT_TEAM.md` | the region block at the very end (`<!-- PROJECT-CUSTOM:BEGIN -->`, the placeholder comment, `<!-- PROJECT-CUSTOM:END -->`) must be EXACTLY the last 3 lines of the file | check 31 (arm C), `verify-template-consistency.sh:2674-2677,2694` (`tail -n 3 templates/general/AGENT_TEAM.md`). Verified: the file's actual last 3 lines are exactly those three markers (checked via `tail -n 5`). If a diet edit adds or removes a blank line inside or around the region, `tail -n 3` stops capturing the whole block and check 31 goes red for reasons unrelated to region content. Survives to the same ownership commit as the check-25/26 rows above. |
| `templates/general/.claude/agents/coder.md` | must have a `<!-- PROJECT-CUSTOM:BEGIN -->` region (used as the third fixture file in check 31's copy step) | check 31, `verify-template-consistency.sh:2645` (`cp templates/general/.claude/agents/coder.md ...`) — out of this task's three named files, but coupled to the same check; note only, no diet action implied. |
| `templates/{dotnet,dotnet-maui,rust-tauri,java,python}/CLAUDE.md` | literal `\.claude/rules/` (any occurrence) | check 19, `verify-template-consistency.sh:670-676`, skips `general` variant. Grep: `grep -q '\.claude/rules/' "templates/$v/CLAUDE.md"` (for non-general variants only). Hit count per variant: dotnet=1, dotnet-maui=1, rust-tauri=1, java=1, python=1. |

## Skill names `hooks/require-skills-block.sh` greps (full `REQUIRED=` sets, `:99-152`)

Read directly from the hook's `case "$SUBAGENT_TYPE" in` block:

- `coder|*-coder` → `karpathy-guidelines superpowers:test-driven-development superpowers:verification-before-completion superpowers:receiving-code-review` (`:102-104`)
- `tester` → `superpowers:systematic-debugging superpowers:verification-before-completion superpowers:test-driven-development` (`:110-112`)
- `architect` → `superpowers:writing-plans superpowers:brainstorming` (`:119-121`)
- `ops|Explore|code-reviewer` → deliberately exempt, no `REQUIRED` set, `exit 0` (`:150-152`)
- `*` (unknown/consumer-added agent, e.g. `game-tester`) → passes through, `exit 0` (`:159-161`)

These skill-name strings are exactly what check 10's `check_pair` cross-references
against `templates/general/AGENT_TEAM.md` — the table above already lists the
pairs; this section is the primary source they were read from.

## `hooks/tier-before-coder.sh` — does not exist

The brief instructed reading `hooks/tier-before-coder.sh:60-90` for "the
plan-template lines it greps." Verified 2026-09-05: `hooks/` in this worktree
(base commit `5d7bbb1`) contains no `tier-before-coder.sh` — `ls hooks/` lists
`agent-budget-warn.sh, bash-output-guard.sh, deny-secret-reads.sh,
enforce-agent-contract.sh, enforce-delegation.sh, gate-before-merge.sh, lib,
no-push-main.sh, pre-commit-test.sh, read-size-gate.sh,
require-skills-block.sh, retro-brief.sh, retro-ledger.sh, run-gate.sh`. This
matches check 6's own commentary (`verify-template-consistency.sh:83-88`): the
plan gate — and the hook that enforced it — was removed in v2.1 PR7. The only
live requirement involving the string `tier-before-coder` is the absence
assertion already captured in the `GATE_LITERALS` rows above (the name must not
reappear in prose). No separate "lines it greps" row exists to add; this is a
measured deviation from the brief's Step 1 file list, not a silent omission.

## `CLAUDE.local.md` — zero rows, verified

`grep -c "CLAUDE\.local\.md" scripts/verify-template-consistency.sh` returns
`0`. `hooks/enforce-delegation.sh:14,69` names `CLAUDE.local.md` only inside a
comment/error-message listing the PO's write surface — it never greps the
FILE'S CONTENT. So `CLAUDE.local.md` carries no literal-preservation
constraint from either the consistency script or any hook: the diet is free to
cut its content without a companion check edit. This is stated explicitly
(rather than silently leaving the file off the table) so an absent row here
reads as "checked and clear," not "not examined."

## `user-level-reference/CLAUDE.md` — same two arms, separate branch

Checks 2 and 3 also assert against `user-level-reference/CLAUDE.md`
(`verify-template-consistency.sh:38` for `superpowers:`, `:54` for the
Superpowers-skills header) in addition to the six `templates/*/CLAUDE.md`
copies. A diet pass over the templates that misses the user-level-reference
copy of the same two literals will still fail the gate — flagged here so a
propagated cut doesn't stop at the templates directory.

## Check 34 — no template-file dependency

Check 34, `verify-template-consistency.sh:2944-2971`, runs a heading-anchored
placement algorithm entirely against its own committed fixture
(`scripts/fixtures/b3-placement/{base,template,merged,spliced-negative-control}.md`),
not against any `templates/*` file. Confirmed by reading the block: no
`templates/` path appears in it. No literal-preservation row needed for the
three diet files from this check.

## Fix round 1 (2026-09-05)

**Critical gap found by task reviewer:** Check 19 (`.claude/rules/` pointer literal
in non-general variants' `CLAUDE.md`) was missing from the table.

**Row added:** 
```
| `templates/{dotnet,dotnet-maui,rust-tauri,java,python}/CLAUDE.md` | literal `\.claude/rules/` (any occurrence) | check 19, `verify-template-consistency.sh:670-676`, skips `general` variant. Grep: `grep -q '\.claude/rules/' "templates/$v/CLAUDE.md"` (for non-general variants only). Hit count per variant: dotnet=1, dotnet-maui=1, rust-tauri=1, java=1, python=1. |
```

**Grep verified:**
```bash
grep -c '\.claude/rules/' "templates/dotnet/CLAUDE.md"        # 1
grep -c '\.claude/rules/' "templates/dotnet-maui/CLAUDE.md"   # 1
grep -c '\.claude/rules/' "templates/rust-tauri/CLAUDE.md"    # 1
grep -c '\.claude/rules/' "templates/java/CLAUDE.md"          # 1
grep -c '\.claude/rules/' "templates/python/CLAUDE.md"        # 1
```

**Commit:** `6eee9fbd84e6...` (to be written)
