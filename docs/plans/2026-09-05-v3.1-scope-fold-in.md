# v3.1 scope — folding the v3.0.3/v3.0.4 backlog into the specs

**Status:** scope document for the consumer challenge round, 2026-09-05. Companion to
`2026-09-03-v3.1-ownership-model-spec.md` (the design), `2026-09-03-v3.1-template-diet.md`
(the prerequisite plan) and `2026-09-03-v3.1-gate-checked-branches-spec.md` (the candidate).
Source of the items: `docs/plans/2026-09-05-next-version-backlog.md` on `main` (merged as
`5d7bbb1`), every item measured by a consumer session against v3.0.3 or the v3.0.4 tip.

Three things this document does: (1) says, for every backlog item, whether the ownership
model **absorbs** it, whether it is **new v3.1 work** and in which repo, or whether it is
**deferred** with the reason; (2) records three places where the diet plan and the ownership
spec, both dated 2026-09-03, **contradict each other**, with the resolution; (3) lists the
questions the challenge round puts to each consumer.

## 1. Reconciliations between the two 2026-09-03 documents

**R1 — the diet plan diets a file the ownership spec stops shipping.** Diet Task 4 cuts
`CLAUDE.local.md` to ≤ 6 KB in all six variants. Ownership §3(a)/§5 removes it from the
template and redistributes its content: MCP rules → `template`-owned `.claude/rules/mcp.md`,
SQLite rules → path-scoped `.claude/rules/sqlite.md`, open-brain section dropped.
*Resolution:* Task 4 becomes "redistribute, then stop shipping" — the content contract in
its table still applies to what lands in `rules/mcp.md`; `BUDGET_CLAUDE_LOCAL_MD` is not
created; check 33 budgets `CLAUDE.md`, `AGENT_TEAM.md` and the new `rules/mcp.md` instead.
The SQLite move (Task 4 row 5) is unchanged and is the same file ownership §5 names.

**R2 — the diet plan's migration note describes the world the ownership spec abolishes.**
Diet Task 5 Step 1 says a sync will report CONFLICT on `CLAUDE.md` and consumers should
re-paste customs into the PROJECT-CUSTOM region. Ownership §7 replaces CONFLICT entirely and
§5 removes the region markers. *Resolution:* the diet ships in the same v3.1.0 as the
ownership model; the CHANGELOG migration note is ownership §11, and Task 5 Step 1 item (2)
is replaced by a pointer to it. The finding-56 sentence and the ratchet paragraph stay.

**R3 — the literal-preservation table protects markers the ownership spec deletes.** Diet
Task 0's table lists the `<!-- PROJECT-CUSTOM:BEGIN/END -->` markers as must-survive
(required by `sync-template/region.sh`). Under ownership the markers go and `region.sh`
loses its purpose. *Resolution:* the markers survive **until the ownership commit**, which
removes them, `region.sh`, and the region checks in the same commit, so the consistency
script is never red between. Task 0 notes the ordering.

**R4 — repos line.** Ownership's header says the `sync-template` skill body lives in
`mcp-dev-servers`. It lives in this repo at `user-level-reference/skills/sync-template/`
(edited in v3.0.4); the server repo holds the MCP tools and the skill's *version contract*.
Corrected in the spec header.

**R5 — §10 "no hook or `hooks/lib/` change" no longer holds.** v3.1 carries the
`git-cmd.sh:937` fix (§3.1 below), which is in the lib. The parser matrix is therefore
mandatory before the v3.1.0 tag regardless of what else ships, and the gate-checked spec's
"prefer a sibling function to avoid the matrix cost" argument no longer applies — the matrix
is paid once either way, so the gate-checked parser may live in the lib beside
`gc_protected_branches`.

## 2. Absorbed by the ownership model (no separate work; the challenge round confirms)

| backlog item | absorbed by | what the consumer should confirm |
|---|---|---|
| Sync three-way merge placement-aware correctness (yutraffic b3: a Gate/Test comment landed 30 lines from the field it annotates, `has_conflicts:false`) | §4/§5: `PROJECT_CONTEXT.md` is `once`, never merged; the **key audit** reports missing keys and unfilled values and never inserts | yutraffic: does "reported gap, never inserted" cover the case the fixture encodes? The fixture (`scripts/fixtures/b3-placement/`, check 34) stays as a regression test on the old path until the merge code is deleted, then retires with it |
| Keep-mine splice anchors differ per project (MM-Agent) | same: no splicing into a `once` file | MM-Agent: which keys the audit must report for your tree |
| `region_preserved:false` on a placeholder-only region reads as alarming (penumbra 4) | §5: markers removed; no region status exists | penumbra: nothing to confirm beyond the migration shape |
| `lastSyncedVersion` ownership (server-written vs skill 7b) | §6: manifest v3 carries `template_version`, **written by the server at finalize**; the client-side 7b stamp is removed | open-brain, panoscribe: your measured cases (stale v3.0.2 stamp; finalize preserving unknown keys) are the server tests for this field |
| Two consumers hold on old versions because sync is a hand-merge | §1 problem statement | penumbra, MM-Agent: is the migration shape in §7 what would make you sync on release day |

## 3. New v3.1 work, by repo

### 3.1 Toolkit — hooks and lib (parser matrix mandatory)

1. **`git-cmd.sh:937` `-C` fallback + class-3 on read-only verbs** (penumbra, yutraffic).
   Two refusals on `git -C <path> merge-base`: the unanchored `\bgit\b.*\b$2\b` fallback
   classifies `merge-base`, `--grep=merge`, `docs/merge.md` as the verb; and the class-3
   unresolvable-`-C` refusal fires before the verb is known. Both must go for §1b of the
   sync skill (`-C <templateRepo>` + descendant reasoning) to work as written. Fix shape:
   after `-C <operand>` and globals, match the verb as the first non-option token with arm
   1's anchor; resolve the subcommand first and refuse an unresolvable `-C` only for gated
   verbs. Fixtures: penumbra's four rows + want-2 controls (`git -C <path> merge feature`,
   `git -C <path> push origin main`), plus a third and fourth 0-control
   (`git -C <path> log -- docs/merge.md`, `git -C <path> merge-tree A B`); yutraffic's
   `git -C "$VAR" cat-file` → 0. Panoscribe ran five arms on protected main (A
   `merge-base --is-ancestor`, B same without `-C`, C `merge f/x`, D `merge-tree A B`, E
   `checkout feature/merge-fix`) — arm E is CONFOUNDED (branch-change logic at :834-837)
   and is not a control; only A-D count. **Warm-up branch `v3/v31-warmup`, in flight.**
2. **`last-precommit.json` overwritten by every Bash call** (MM-Agent): write the artifact
   only when a commit segment was found, or keep the last N rows; add a
   `matched-inside-quoted-word` note when the commit verb matched inside a quoted token
   (their 615 s false-positive Test run). `pre-commit-test.sh` only; no lib. Three
   consumers now (MM-Agent, yutraffic): yutraffic's post-commit read showed
   `no-commit-segment, rc -1, tree "", cmd_len 545` — their own inspection command
   overwrote the row. Inspecting the artifact is itself what destroys it.
3. **Commit gate keyed to the payload cwd, not an in-command `cd`** (controller, measured):
   document in the hook header and `docs/verification.md`; consider refusing a commit segment
   preceded by a `cd` clause, same shape as the mover-clause rule. Design question for the
   round: is refusal right, or is "gate the cwd the payload names, say so in the artifact"
   enough?
4. **Pipe-refusal wording blames the wrong clause** (penumbra 2): the discriminator calls
   `tail` an "earlier clause" when it is the pipe's later one, and a compound with a
   `checkout` in the same call should say "evaluated from `main` because the checkout is in
   the same call". Text only, in `gate-before-merge.sh` / `no-push-main.sh`. Penumbra's
   exact sentences: compound — "refused: <verb> evaluated on branch 'main' — the
   'git checkout -b <X>' earlier in this call has not run when this hook fires; split the
   call: checkout first, then <verb> alone."; pipe — replace "earlier clause" with "the
   pipe's later stage (`tail`) is not inert".
5. **`post-edit-build.sh`** (ownership §5a): new hook reading `**Post-edit build**:` from
   `PROJECT_CONTEXT.md`; `none` or placeholder → no-op; fixture in `test-hooks.sh`; no lib.
6. **Gate-checked branches** (companion spec, MM-Agent's requirements): rides v3.1 if its
   plan is ready; parser beside `gc_protected_branches` (R5).
7. **`**PO write surface**:` key** (penumbra F12): `hooks/enforce-delegation.sh` reads extra
   path prefixes from PROJECT_CONTEXT.md keyed `**PO write surface**:`; `none` = template
   default; `optional_absent` in the key audit; then the hook returns to `template`.
8. **SubagentStop matcher derived from the project's agents at runtime — REQUIRED**
   (MM-Agent): because `settings.json` is `template` class with no merge class, the v3.1
   hook derives the SubagentStop matcher from the PROJECT's `.claude/agents/*.md` at
   runtime (consumer-only agents included, e.g. their `mm-runner`/`finisher`), not a
   template-time list — otherwise MM-Agent's two project agents lose their contract hook on
   the first v3.1 sync.
9. **Tracked-only tree hashing** (penumbra F18) — moved here from §3.2 item 11: SHIP in
   v3.1, not a design pass. Would have prevented v3.0.3 item 8 (in-repo commit-message
   file) and the untracked-review-file case; would not prevent mutation-batched-with-commit
   (last-precommit.json's discriminator, already shipped). `add -A` never protected against
   a contaminating untracked file, it only forced a second run in the same tree. Shape:
   copy the real index (keeps staged new files), `git add -u`, `write-tree`. Three-arm
   fixture (`run-gate.sh`, no lib): untracked unignored file present → artifact tree ==
   `HEAD^{tree}` (match); unstaged edit to a tracked file → mismatch (control);
   `git add new.py` → gate → commit → match. Second reason (yutraffic): on a new branch's
   first commit the SHA arm can never match — PreToolUse runs before the commit exists
   (measured: `last-pass.json` sha = pre-commit HEAD, tree = the resulting `HEAD^{tree}`)
   — so the TREE arm is load-bearing, not optional polish, for this shape; reword the
   run-gate header when it ships to say so.
10. **`gate_self_reference` token normalisation** (open-brain RB1): tokens are compared
    after the same backtick/whitespace normalisation `run-gate.sh` applies to the field
    value — a backticked `**Gate**:` value is valid and must still match.

### 3.2 Toolkit — scripts, templates, docs

7. **`setup-project.ps1:861`** (open-brain): guard like `:153-175`; check 27's ps1 arm
   asserts the exit code and that `.claude/template-manifest.json` exists; open-brain's
   paired-control fixture (sh 31 files + manifest vs ps1 30 files + none on the same
   no-`.git` tree) is the regression test. **Warm-up branch, in flight.**
7a. **`setup-project.ps1` BOM fix** (yutraffic F22): `Set-Content -Encoding UTF8` at
    `:757`, `:770`, `:806`, manifest `:920`, `.mcp.json` `:947` writes PS 5.1's UTF8 = BOM;
    switch every one of those sites to a BOM-less writer
    (`[IO.File]::WriteAllText($p, $s, (New-Object Text.UTF8Encoding $false))`, or the
    existing `:347` byte helper); fixture in `test-setup-project.sh`: after a ps1 bootstrap
    NO written file starts with `EF BB BF` (the sh/ps1 byte-equality fixture at `:861`
    already asserts the trees match, so a reintroduced BOM would fail that one too — the
    two fixtures reinforce each other).
8. **`VERSION` line 2** (controller): consistency check that line 2 differs from the
   previous tag's; the two-line convention is documented in the diet plan Task 5 and gets a
   sentence in `CLAUDE.md`'s Gate section.
9. **Identity-transfer rule → `docs/verification.md`** (yutraffic, panoscribe): a verdict
   transfers when every differing byte is accounted for — ancestry for fast-forwards, tree
   equality of every path the verdict rests on for squash merges; hooks by hash, `SKILL.md`
   and `test-hooks.sh` by re-read; say which. Paired with: the crisp version of a rule is
   usually the one that has not met the second case yet.
10. **Ownership table + consistency check** (ownership §6): every template file has a class,
    no variant ships `CLAUDE.local.md`; delete-the-guard row (remove a class → red). Also:
    the sentinel consistency check (six variants carry the `context-mode` line + a grep of
    the plugin bundle for its `includes("context-mode")` predicate) — the root
    `templates/*/CLAUDE.md` files get the sentinel line; so does this toolkit's own root
    `CLAUDE.md`, which ends the ` M CLAUDE.md` hazard this repo has carried since the
    plugin was installed. Also: the ownership.json guarantee row (every pattern matches
    ≥1 template-tree file, none is a bare root `*.ext`; server test from batch 2 item 6).
11. **finding-59 provenance channel** (`RUN_GATE_TERMINAL` across the Test `eval`; clamp on
    `rc==78 && marker`): design pass item; changes what the gate accepts. Decide in the
    round whether it ships in v3.1 or waits. (Tracked-only tree hashing, formerly bundled
    here with `add -A`, is now §3.1 item 9 — SHIP in v3.1, not a design pass.)
12. **Profiling** (open-brain): `gate-before-merge` ~+700 ms, `no-push-main` ~+350 ms since
    v3.0.2; re-measure after the lib change in 3.1.1 with the same paired-interleaved method.

### 3.3 Toolkit — `sync-template` skill body (rewritten against §7 statuses)

13. **`deletedAcknowledged` is read by nothing** (MM-Agent, mcp-dev-servers): the contract is
    `template_finalize_sync(deleted_files=[...])` (already 13 `deleted_files` at finalize);
    6a/7b drop the stamp; state the auto-drop case (template stopped shipping AND project
    removed it).
14. **Probe of the project `deny-secret-reads` copy needs a script file when the user-level
    hook is installed** (MM-Agent): 6b warning — a probe of the hook, not a bypass recipe.
15. **Prediction-committed-first needs a branch-provenance clause** (penumbra 8): the
    prediction commit must be made from a branch whose commit-minted artifact matches, or
    the sync branch's first commit fails the merge gate (`add -A` hashes untracked files).
    Reworded per penumbra: "prediction commit made alone in its own call, on the branch the
    sync will use, with last-precommit.json tree == HEAD tree" (if item 9/§3.1 ships,
    `add -A` is no longer the cause).
16. **Step 8 report's "Sync server:" line has no source** (penumbra 7): `template_load_manifest`
    echoes `server_version`; the report template reads it (server half in 3.4).
17. Retire what ownership makes moot: 2b's `.gitignore` control (no more merge into tracked
    files), step 4 placement check, step 6b applied-set, 7b stamp, `region.sh`. Each
    retirement names the v3.0.4 item it closes so the record stays traceable.
17a. The duplicate-load check extends to `CLAUDE.local.md` (today CLAUDE.md only) and names
    which headings duplicate template-owned rules (yutraffic F21, panoscribe).
17b. The step-9 sentinel guard checks the file the plugin actually targets, not always the
    project root — nested layouts mean it may be a PARENT `CLAUDE.md` (panoscribe:
    `grep -c '^# context-mode'` in their repo → 0, in the parent `G:/git/CLAUDE.md` → 1;
    the plugin appends to the nearest CLAUDE.md, so the guard's premise fails on a naive
    project-root check for nested repos).
17c. The sync report adds a `restart_required` line (derived from whether agents/skills
    changed — MM-Agent row 5).
17d. Post-sync step: when the report carries `gate_unverified`, tell the consumer to run
    `bash hooks/run-gate.sh` once and treat exit 78 with the self-reference marker as the
    loop (open-brain).
17e. **Penumbra A (first v3.0.4 sync):** SKILL.md §1b case 2 says "rewrite lastSynced to
    the TAG COMMIT before 7b (or have finalize record it)" — the second arm does not exist
    until server 0.3.0: `template_finalize_sync` takes no commit argument today and
    records the checkout HEAD unconditionally. Mark the parenthetical "server 0.3.0"
    (or drop it) until then.
17f. **Penumbra C:** the `Toolkit:` report line between finalize and 7b prints
    `v3.0.3 (5d7bbb1) STALE LABEL` correctly, but a crash in that window leaves a manifest
    naming the wrong release; 7b states the finalize→7b stale-label window explicitly and
    that the stamp closes it.
17g. **Penumbra D:** `no-push-main` keys on the PUSH TARGET, not the current branch — the
    probe now runs pre-merge on a feature branch by construction; one sentence in the skill
    body.
17h. **Yutraffic GAP 1:** the bash→python `/tmp` warning is in the intro, 2b and 7b but NOT
    at 6b — where it bit twice (a `/tmp/all.txt` staging file was itself the step's own grep
    pipeline output). 6b should say: derive the referenced set inside ONE process, never
    stage it through `/tmp`.
17i. **Yutraffic GAP 2:** a "no unfilled placeholder in my spliced output" check
    false-positives on PROJECT_CONTEXT.md — the protected-branches comment legitimately
    contains a literal `{{…}}` while explaining fallback. Step 4 sends the reader to step
    7's arm-1 pattern (`- **Key**: value`), not a bare `{{` test: this file documents
    placeholders in prose.
17j. **Step-9 promotion (yutraffic):** between `git checkout main` and the fast-forward,
    local main runs the PRE-sync enforcement layer — harmless for a text release, not for
    a security one. The note ("local main carries the pre-sync hooks until it fast-forwards")
    is promoted OUT of the code block; a consumer who cannot complete the pull sits in that
    window indefinitely.
17k. **Step 7/1b key audit reading** (yutraffic RB3): required keys are matched with the
    hook's own regex exactly (`**Key**:` or its deprecated spelling), never qualified; a
    qualified-only holding is reported as `qualified_only`, not present; `Test` is optional
    everywhere (rust-tauri's Gate-only fallback is the documented default).

### 3.4 Server — `mcp-dev-servers` (first, backward compatible; §9)

18. Manifest v3 read/write; three classes; statuses in §7; migration in §7 with the vacuity
    control in §8; `template_version` written at finalize (absorbs `lastSyncedVersion`).
19. `template_get_diff` accepts `diff_type "unified"` or drops the `unified_diff` field-name
    mismatch (penumbra 3).
20. `template_load_manifest` returns `server_version` (penumbra 7).
21. Shipped already: atomic-write retry with `.tmp` cleanup — **v0.2.1**. The `requires_server`
    floor in §6 is the version that carries items 18–20; the server owner names it before
    either implementation plan is executed (§6 says so; the number goes here: `>= ____`).
22. **0.3.0 items from the six server batches, by name:** ownership loader with
    `tracked_paths`; manifest v3 + gate; `template_migrate_manifest` incl. `dry_run`; key
    audit lists (`required_keys`, `deprecated_keys`, `aliases`, `placeholder_map`); orphans;
    `redundant_project_file`; `gate_self_reference`/`gate_unverified`; `region_was_seed`;
    `encoding_drift`; `unknown_keys`; the `template_get_diff` `unified` alias;
    `server_version`. Server implementation plan written and **APPROVED by their user**
    2026-09-05: `mcp-dev-servers/docs/plans/2026-09-05-v3-ownership-implementation.md`;
    batches 4–5 (normalisation, `gate_self_reference` addendum) folded in before execution.

## 4. Deferred, with the reason

- **Merge-gate `-c` classifier asymmetry** (inside the protected-branch condition in
  `gate-before-merge`, before resolution in `no-push-main`): documented per-hook; revisit
  only if the gates are unified. Not v3.1.
- **`*`/`?` glob operands in `-C` paths**: owed by a Linux consumer; NTFS cannot produce them.
- **Plugin packaging**: v4 (ownership §10).
- **`user-level-reference/CLAUDE.md` and agent-file diet**: a second diet plan (diet plan
  self-review).

## 5. Patch rule, restated

No v3.0.5 unless a security bypass or a gate-integrity defect appears. Everything above is
v3.1. A consumer who needs an item sooner asks for it by name and it is weighed against that
rule, not against convenience.

## 6. Challenge round — what each consumer is asked

The design is canvassed before any diff (consumer review protocol): each consumer runs the
shapes against files only they can see and reports what they measured, not what they think.

**Every consumer (five), read-only against your own tree:**
- List every file you hold as keep-mine / PROJECT_CUSTOM and classify each per §5a: variant
  gap (name the variant and the missing template content) or per-project value (name the
  key it should become). Anything that fits neither is a finding against §5a.
- List your out-of-region edits to `CLAUDE.md` (diff against the template at your held
  version). They are the migration fixture (§8) nobody else can supply. Say whether the
  fenced-hunks shape in §7 step 3 is acceptable for them.
- Confirm what in your `CLAUDE.local.md` you rely on, and that nothing breaks when the
  template stops shipping it (your copy stays, untouched, `project` class).
- Which `.claude/rules/*.md` you already have, and whether a `once`-class
  `.claude/rules/project.md` loading at launch collides with anything.
- For the items in §2 attributed to you: does the absorption hold, or is something lost?

**MM-Agent:** the gate-checked branches spec is yours (finding 56) — do the four
requirements still hold, and is the `m*-session-*` glob still the right shape? Your
`settings.json` keep-mine (Read(.env*) deny rules kept deliberately, post-edit `dotnet
build`): §5a says the build hook is a variant gap fixed by `post-edit-build.sh`; classify
the deny rules. Items 13, 14, 2 are yours to re-measure when they land.

**penumbra:** items 1, 4, 15, 16, 19 are yours; confirm the mapping and the wording of the
compound-command sentence you want. Your v3.0.3 item 8 (`add -A` hashing untracked files)
also bears on §3.1 item 9 (tracked-only tree hashing, moved from 3.2 item 11) — say whether
tracked-only hashing would have prevented it.

**yutraffic:** §2 row 1 — is "reported gap, never inserted" the right replacement for a
placement check, or is there a case the key audit cannot see? Your `git -C "$VAR" cat-file`
row is the class-3 fixture for item 1; the b3 fixture retires with the merge code.

**open-brain:** item 7 is yours; your paired-control fixture is its regression test. On §6:
your measurement that finalize PRESERVES unknown manifest keys — does manifest v3 need to
drop unknown keys, or preserve them and say so?

**panoscribe:** items 8, 9 and the suite-verdict rule are yours to read once they land;
on §7, the `LOCAL_EDITED` status writes the diff to the step-2b backup dir and overwrites —
is a diff-in-a-backup-dir enough evidence for a consumer to recover an edit, or does the
status need to name the hunks in the report?

**Silence is not clearance.** Each consumer's verdict on the design is explicit, or the
round is not closed.

## 7. Challenge-round ledger

The record. Copied verbatim from `challenge-round-findings.md` (the measurement source for
every item folded into §1–§4 above and into the ownership spec's amendments), with its two
`a72b531c` mentions corrected to `a72b531a [controller typo, corrected]`.

---

# v3.1 design challenge round — findings ledger (spec a72b531)

One entry per consumer verdict as it arrives. Each finding: what was measured, what the spec says today, the resolution to write into the spec (or "open"). Becomes the next amendment commit on `docs/v31-specs` when all five have reported.

## MM-Agent (2026-09-05, tree at 86561fe / v3.0.3, read-only) — design verdict: PASS with findings

**Gate-checked branches spec: PASS, no changes.** Branches `m106-session-2026-09-02`, `m113-session-2026-09-03`; `m*-session-*` right, does not match `worktree-agent-*`; four pushes/day so artifact-only stays essential; strip-the-line control kept; they run the seven landing-route rows + control before syncing v3.1.

**F1 — key audit must distinguish REQUIRED (hook-read) from OPTIONAL-ABSENT keys.** Their PROJECT_CONTEXT.md has 20 template keys + 9 project-only keys, no `**Gate-checked branches**:` line. Required, report as MISSING: `**Protected branches**:` (10 hook read sites), `**Gate**:` (8), `**Test**:` (6). Optional, report as "absent = default applies": `**Post-edit build**:`, `**Gate-checked branches**:`. Project-only keys ignored, never flagged. *Spec change (§5 key audit, §6 ownership.json):* the template's PROJECT_CONTEXT.md marks optional keys (or the ownership rule carries `"required_keys": [...]`); the audit reports three lists: `missing_required`, `optional_absent`, `placeholder_keys`.

**F2 — legacy key spellings.** `**Gate Command**:` / `**Test Command**:` still have 3 read sites in the hooks; undocumented second spelling. *Resolution:* the audit reports them as deprecated aliases; v3.1 hooks keep reading them (no behaviour change) and the CHANGELOG names the deprecation; removal is a later release.

**F3 — settings.json keep-mine, measured key-by-key against templates/dotnet: exactly two deltas.**
(a) SubagentStop matchers extended with `^mm-runner$|^finisher$` (their two project agents) on enforce-agent-contract and the PIPELINE echo. Classification: PER-PROJECT VALUE; the natural key is the agent list itself — derive the matcher from `.claude/agents/*.md` names, or a declared `**Pipeline agents**:` key. *Spec change (§5a pass):* settings.json returns to `template` only if the SubagentStop matcher stops being a literal list; decide derive-from-agents vs declared key in the toolkit plan.
(b) Six `Read(.env*)` deny rules kept deliberately as a transitional duplicate of `deny-secret-reads.sh` (which they wire on Read|Bash exactly as the template does). Classification: NEITHER — a finding against §5a. The template cannot ship them (v3.0.3 removed them because deny rules are evaluated before the auto-mode classifier and prompted on ordinary reads). *Resolution:* §5a gains a third, time-boxed category — **transitional duplicate with a stated retirement condition** — and the skill says "delete after one measured week with the hook live". A file held only for a transitional item stays `template` class; the sync report names the duplicate each time until it is gone.

**F4 — CORRECTION to the spec's §5 premise.** MM-Agent has NO post-edit `dotnet build` hook; nothing in their PreToolUse/PostToolUse runs dotnet. The "one consumer (dotnet) holds settings.json PROJECT_CUSTOM for a post-edit build hook" claim in §5/§5a is not theirs. *Resolved (measured 2026-09-05, Read tool):* the consumer is **InvestmentAdvisor** (`G:/git/InvestmentAdvisor/.claude/settings.json`): PostToolUse `Edit|Write` → `dotnet build --no-restore -v q 2>&1 | tail -20`, on a pre-v2.0 config (`defaultMode: dontAsk`, `Bash(*)`, `WebFetch(*)`, one `Read(.env*)` deny, `mcp__git-tools__*`). A real consumer, not one of the five sessions, and not synced since v1.x. *Spec change (§5):* name the source, and state that the restoration is justified on its own — the dotnet variant shipped this hook before v2.0 made all six settings.json byte-identical. InvestmentAdvisor is also the natural fixture for the v1/no-manifest adoption path of the migration (§7) — ask its owner (the user) before using its tree.

**F5 — CLAUDE.md out-of-region edits: ZERO** (manifest `localPartHash == templatePartHashAtSync`, `region_only true`). No migration fixture from MM-Agent; fenced-hunks shape accepted in principle, unmeasured.

**F6 — CLAUDE.local.md is byte-identical to the template** and relied on every turn (git-CLI gates table, open-brain mandate, dotnet-tools MUST rules, GitHub tool-or-gh contract). Nothing breaks when it becomes `project`. *Two consequences for the spec:* (i) a consumer whose CLAUDE.local.md equals the held template version will hold a stale duplicate of what moves to template-owned `rules/mcp.md` — the migration should report `redundant_project_file` for a byte-identical copy and suggest deleting it (never delete); (ii) hazard: a project-class file that names hooks and their behaviour drifts silently when hooks change — the audit cannot read project files by §4, so the answer is (i): make the copy disappear by the consumer's hand rather than keep it current.

**F7 — `deleted_files` at finalize creates ORPHANS, and §4 says the server never lists project files.** Dropping the two kept agents (requirements-engineer.md, test-writer.md) from the manifest makes them present-on-disk, absent-from-manifest — the class step-2b's third backup set exists for, and how game-tester.md went invisible. MM-Agent asks that every sync list orphans under manifest-tracked directories (`.claude/agents/`, `hooks/`, `.claude/rules/`). *Tension with §4 ("the server never reads, writes, or lists" project files). Resolution:* §4 wording becomes "never reads or writes; LISTS them only as `orphans` under template-owned directories, informational, never acted on". The server owner must confirm the cost (one directory walk).

**Unknown manifest keys preserved + reported: right.** Rules: only `.claude/rules/csharp.md` (template); a `once` `project.md` collides with nothing.

**§2 rows attributed to MM-Agent:** row 2 (keep-mine splice anchors) — answered by F1: an audit line would have replaced the hand splice; row 5 (holding on old versions) — asked.

## Server owner answers to batch 2 (penumbra findings) — their review §6
(1) base = `git show <sha>:<_template_file_path(manifest,"CLAUDE.md")>` = `templates/<variant>/CLAUDE.md`; root-tracked hooks/** at repo root. (2) `_apply_placeholders` → `_split_custom_region` → diff template parts, same rule as hashes. (3) `template_version` algorithm: `git tag --merged <template_commit> --sort=-v:refname`; per tag compare tree ids over every tracked path; first all-equal tag wins; never describe; none or not a git checkout → null + `untagged_template_tree`. COUNTER ACCEPTED: `"tracked_paths": ["hooks","scripts","templates","user-level-reference"]` lives in ownership.json (toolkit layout, not server knowledge); missing key → server falls back to templates/<variant> + root-tracked pattern prefixes and warns. (4) fallback chain `template_commit` (alias lastSynced) → `template_version^{commit}` → `migration_base_unavailable`, existence via `git cat-file -e`; the skill's `git fetch --tags` is what makes the fallback reachable — state in §9. (5) `template_migrate_manifest(project_path, backup_dir="", dry_run=false)`: dry_run writes nothing, returns would-be project.md, hunk_count, migration_base, redundant_project_file, warnings, would-be v3 manifest. (6) classification scope stated precisely: template-tree paths + manifest entries (mapped back through target); consumer file with no entry = project regardless of pattern, shows only in orphans when matching a template-class pattern.

## Server owner answers to batch 3 (yutraffic key-audit findings) — their review §7, all accepted
(a) per required key: `value`, `template_value` (current, placeholder-replaced), `template_value_at_sync` (held base, same fallback chain), `template_default_changed` = template_value ≠ template_value_at_sync, `consumer_holds_old_default` = value == template_value_at_sync (never customised → moved default probably wanted); base unavailable → flags omitted + `audit_base_unavailable`; whitespace-normalised; never writes. (b) key K present when consumer holds exact K, qualified `^K \(.+\)$`, or a name in the rule's `"aliases": {"Build": [...]}`; reported under `matched_as`; first match resolves (a)'s value; same lookup for required/optional/deprecated. (c) `template_notes_changed` only for once rules with `"audit":"keys"`: diff placeholder-replaced template at held base vs current; hunks touching no `**Key**:` line reported with text, never applied; non-audited once files (PROJECT_STATE.md) stay never-diffed; base unavailable → `audit_base_unavailable`. (d) `template_migrate_manifest` returns `project_md_bytes`; threshold/warning skill-side; server never fails on size. Extra once rows need nothing server-side.

## yutraffic (2026-09-05, rust-tauri, v3.0.3 @ 86561fe, main b2ef758, read-only) — APPROVE WITH THREE CONDITIONS + one defect
**Controller error:** I cited the sha as `a72b531a [controller typo, corrected]`; it is `a72b531a`. Correct everywhere.

**F10 confirmed independently (their F1): the context-mode sentinel.** Their CLAUDE.md:169 comment is the ONLY `context-mode` occurrence; byte-identical to template (12583 B) and never re-appended; `G:\git\CLAUDE.md` without the sentinel carries the full block. `context-mode` appears 0× in the ownership spec and 0× in the diet plan. Under v3.1 as written: permanent unauthored LOCAL_EDITED, invisible to the four consumers without the plugin. Fix: one line naming the plugin stays in template CLAUDE.md (controller choice — it must be in the TEMPLATE-owned file, because the writer targets CLAUDE.md, not project.md) + literal table + consistency check; re-confirm the plugin's exact predicate at implementation time (recorded mechanism, not a fresh read of the plugin source).

**F11 confirmed independently (their C1): VERIFICATION_PLAYBOOK.md.** keep-mine, 83-line diff, eleven project-authored rules from the #197–#208 epic; CLAUDE.md's Verification section points at it; 0 mentions in spec, diet plan, fold-in. "Project-authored knowledge inside a template-shipped doc — the third case, exactly what the region existed for; §5a's dichotomy is right for settings.json; it does not close." **DECISION (two consumers, same finding): `once`.** Template rule updates to the playbook arrive via CHANGELOG, not overwrite. *Also unclassed and tracked:* `.gitattributes`, `.prettierrc`, `rustfmt.toml` (variant style files; they do not deviate) → must appear in ownership.json — controller lean `once` (style configs the project tunes). `.claude/rules/{lang,…}` is plural: rust.md AND frontend.md, both path-scoped → table wording "`.claude/rules/*.md` except `project.md` = template".

**F19 (their C2) — the key audit as specified is blind to the case the b3 fixture encodes and noisy on everything else.** Measured: (1) the b3 hunk 60e4b0d→86561fe on PROJECT_CONTEXT.md is one HTML comment about how Gate/Test are parsed — zero key lines; a template change to how an existing key is PARSED is invisible to a key audit by construction. (2) template @5d7bbb1 `**Protected branches**: main master`, theirs `main` — present both sides, no placeholder → audit clean, yet the value is hook-read and the default moved. (3) every recurring row on their tree is a false alarm: `**Build**` / `**Test (frontend)**` "missing" while they hold variant supersets (Build (desktop), Build (web only), Test (frontend unit/e2e), Dev (desktop), Dev server); 21 template keys vs 25 theirs; none of the eight rows hook-read. *Spec change (§5/§7 key audit):* (a) for `required_keys` (hook-read) report the RESOLVED value and flag `template_default_changed` when the template's value at the held version ≠ at the target (informational); (b) a template key is satisfied by a consumer key that starts with it followed by " (" (superset/qualified form) — or the rule declares `aliases`; (c) template-comment changes on a `once` file are reported as `template_notes_changed` with the hunk, never inserted. An audit whose every recurring row is unactionable trains consumers to skip it.

**F20 (their Q2) — yutraffic IS §8's vacuity control on a real tree:** CLAUDE.md byte-identical at held version, 0 placeholders, region placeholder-only → migration must emit no hunks and a header-only project.md. Two structural notes: project.md has no `paths:` so it loads every launch, and the fenced-diff section has no size budget in a release about 6 KB budgets → the migration reports project.md's byte size and warns above a threshold (consumer-owned, so warn not fail).

**F21 (their C3) — partial duplication of CLAUDE.local.md vs template-owned rules/mcp.md.** Their copy is 10387 B, gitignored, 29 headings — not byte-identical (unlike MM-Agent/penumbra), so `redundant_project_file` will not fire; the redistributed MCP sections will exist twice, always-on, drifting. *Skill change:* step 5's duplicate-load check greps CLAUDE.local.md too (today CLAUDE.md only), or the migration offers to trim the redistributed sections (report which headings duplicate rules/mcp.md). Dropping CLAUDE.local.md from the manifest is a STRICT WIN for them: today it is gitignored AND manifest-tracked, so an overwrite is unrecoverable.

**Classified list:** CLAUDE.local.md → project (win); PROJECT_STATE.md → once; PROJECT_CONTEXT.md → once (removes the b3 class); VERIFICATION_PLAYBOOK.md → neither → now `once`. Rules: frontend.md, rust.md path-scoped; no project.md. `git -C "$VAR" cat-file` → 0 is the class-3 fixture. Observation: vs 5d7bbb1 their tree shows deny-secret-reads.sh and run-gate.sh AUTO_UPDATE (v3.0.4 unsynced, as expected). Full write-up: their scratchpad `v31-yutraffic-verdict.md`.
## yutraffic follow-up — sentinel resolution accepted; a self-correction and a new defect
**F22 — DEFECT: Windows bootstrap writes UTF-8 BOMs that no sync has ever seen.** yutraffic byte-compared after saying "no deviation" (compute_status's word, not a measurement): `.prettierrc` and `rustfmt.toml` carry `EF BB BF`; the template copies do not; both arrived in their bootstrap commit a01e374 and have read UP_TO_DATE at every sync v2.0→v3.0.3. `.gitattributes` genuinely identical (its CRs are checkout smudge). **Writer measured (controller, 2026-09-05):** `setup-project.ps1` writes every generated file via `Set-Content … -Encoding UTF8` (lines 757, 770, 806; manifest 920; .mcp.json 947) — PS 5.1's UTF8 = BOM; a BOM-less byte helper exists at :347 (`[Text.Encoding]::UTF8.GetBytes`) for some path. Templates: no BOM. Same class as PROJECT_CONTEXT.md's BOM (removed by hand in yutraffic #228; v2.2.4 made the hooks tolerant via GC_KEY_PRE). *v3.1 toolkit item, beside ps1:861:* write with `[IO.File]::WriteAllText($p, $s, (New-Object Text.UTF8Encoding $false))` (or the :347 helper) everywhere; fixture in test-setup-project.sh: after a ps1 bootstrap NO written file starts with EF BB BF (and sh/ps1 trees byte-equal, which the :861 fixture already asserts — the BOM would have failed that equality, so the two fixtures reinforce). *Spec (§7/§8):* the server normalises SOMETHING before classifying (BOM at least) — state exactly what, or "byte-identical" in §8 means two different things on the two sides of one sentence. Design caution (yutraffic): F1 (plugin re-append) and F2 (setup BOM) are both template-class differences the consumer never authored; as `LOCAL_EDITED` = "reported loss" they would be reported forever on untouched trees — normalise BOM and EOL before classifying and say so in §7.
**Class split (accepted):** `.prettierrc`, `rustfmt.toml` → `once` (rust-tauri only; formatter knobs a project tunes; their gate runs the exact CI prettier invocation) — paired with a content-drift note in the audit (report, never overwrite); **`.gitattributes` → `template`** (all six variants, byte-identical, single purpose; its header says CRLF drift breaks CI and determinism hashes — which are yutraffic's merge stop line; a consumer edit is a bug, and under `once` a future template fix never reaches anyone). No further conditions from yutraffic.

## open-brain (2026-09-05, v3.0.3 @ 86561fe, general variant, read-only) — SHIP with two BLOCKING findings
**Sha correction again:** `a72b531a [controller typo, corrected]` does not resolve; real head `a72b531a6560f1aebf682d67d5fc243a430454b5`. (Controller error, cited to five sessions.)

**F23 — BLOCKING: `hooks/**` = template overwrites a consumer's PROJECT-OWNED gate script and leaves a deterministically broken one.** open-brain's `hooks/run-gate.sh` is keep-mine: a 6-step project gate (node_modules drift, SQL dollar-quote lint, tsc, vitest, prettier, eslint) that never reads `**Gate**:`; their PROJECT_CONTEXT.md:22 says `**Gate**: bash hooks/run-gate.sh`. Under v3.1: template run-gate.sh overwrites theirs; PROJECT_CONTEXT.md is `once` so the now-circular value survives; `skip` refused. Measured paired arms (their real PROJECT_CONTEXT.md in a throwaway repo): ARM 1 template run-gate.sh @86561fe → "BLOCKED: **Gate** must not invoke run-gate.sh itself", exit 78; ARM 0 their script → runs "[1/6]…". Blast radius: commits unaffected (they declare **Test** too); MERGES break (no artifact can be minted). §5a classifies it right (per-project value, key **Gate**:); the gap is §7 — nothing detects that a key's value resolves to the file being overwritten. *Resolution:* `template_migrate_manifest` (and compute_status pre-sync) detects a `**Gate**:` (and `**Test**:`) value resolving to a template-class hook path with local edits and REFUSES with named reason `gate_self_reference` before any write — same shape as the backup_dir refusal. The §5a keep-mine pass over all consumers tests specifically for gate logic living IN a hook script. Their remedy when asked: move the six steps to `scripts/gate.sh`, point **Gate**: there, leave run-gate.sh template — "not doing it before the release decides". **open-brain will not sync v3.1 on release day until `gate_self_reference` is answered.**

**F24 — BLOCKING (small): template-owned CLAUDE.md re-injects unfilled placeholders into an always-loaded file.** `{{BUILD_COMMAND}}`, `{{TEST_COMMAND}}`, `{{FORMAT_COMMAND}}` are in template CLAUDE.md @86561fe and NOT in their manifest `placeholders` (only PROJECT_NAME, PROJECT_NAME_LOWER) — setup-project reports them as "fill manually", a human fills the file, nothing records the value. Scanned all 29 manifest paths: exactly one template-class file affected, CLAUDE.md, the one that loads every turn; PROJECT_CONTEXT.md is once + key audit. *Resolution:* the diet drops the Quick Start command block from CLAUDE.md and points at PROJECT_CONTEXT.md (the declared source of truth) — fixes the regression and saves bytes in the same commit. Alternative rejected: recording hand-filled placeholders at setup (§6 does not do it).

**F25 — migration must drop a PROJECT-CUSTOM region byte-identical to the template SEED.** Their region is 74 bytes = the seed comment `<!-- Project-specific rules, routing blocks, and extensions go here. -->`; §7 step 3 "verbatim" would present the toolkit's own seed as the consumer's content in the one file whose purpose is to be theirs. *Resolution:* compare the region against the seed at the migration base (`git show <base>:templates/<variant>/CLAUDE.md`, already resolved in step 2); identical → omit the body, report `region_was_seed`. open-brain is then a live instance of the vacuity control: header + hunks, no region body. (Fourth fixture shape: seed-region + out-of-region edits.)

**F26 — CLAUDE.local.md byte-identical (8655 B, locallyModified:false): THIRD consumer.** "Existing copy is project — untouched" is the WRONG default when the content is provably not the consumer's: always-loaded, frozen at v3.0.3 while rules/mcp.md evolves, unbudgeted by check 33 — and it hollows out the diet's win for existing consumers (their launch set grows: + project.md + mcp.md, CLAUDE.local.md stays) unless deletion lands. *Resolution (now three-sided: MM-Agent, penumbra, open-brain):* `redundant_project_file` report + OFFER deletion (skill asks the user; never silent). Diet plan states the number: fresh project wins; existing consumer wins only after the stale copy goes.

**Migration fixture from open-brain:** 3 out-of-region hunks (+8/−6): two stale cross-reference rewordings (dead once CLAUDE.md is rewritten — fencing is exactly right for them), one live Quick Start hunk (F24's cause). Fenced-hunks shape ACCEPTABLE and preferred.
**Keep-mine per §5a:** CLAUDE.md (per-project value), PROJECT_CONTEXT.md (per-project, `once` right), hooks/run-gate.sh (per-project value, key **Gate**: — F23). 26/29 clean. **rules/:** none exist; manifest has zero rules entries; everything lands clean; their CLAUDE.md already says conventions belong in rules/*.md — v3.1 makes it true. **Unknown keys:** PRESERVE-and-report, unambiguously; silence was the problem, not the policy; do not offer drop-and-report as co-equal. **Server test to add (§8):** migrating open-brain's manifest shape (v2 + two hand-written unknown keys + 26 clean + 3 resolutions) yields v3 with server-written template_version AND both stale keys in `unknown_keys`. **Item 7:** their paired-control fixture stands; send the warm-up sha.
## open-brain follow-up — `gate_self_reference` as specified misses the recommended remedy (measured, with control)
The refusal is a textual match on the field value; the remedy `**Gate**: bash scripts/gate.sh` puts a project-class path in the field, so a LAZY wrapper (`scripts/gate.sh` = `bash "$(dirname "$0")/../hooks/run-gate.sh"`) is silent to it and the loop is back. Fixture: ARM A wrapper → "BLOCKED: **Gate** must not invoke run-gate.sh itself", exit 78; ARM B `**Gate**: true` → GATE PASS, exit 0 (control). The runtime guard (`RUN_GATE_ACTIVE` exported at run-gate.sh:180-181, inherited at any depth) catches indirection; the static check cannot (`make gate`, package scripts, any depth). *Resolutions adopted:* (1) §7 names the static refusal honestly — **direct** self-reference — so a silent pass is not read as clearance; (2) add `gate_unverified` to the migration/sync report: the new `**Gate**:` value was never executed by the sync (running a consumer's gate at sync time is too invasive — MM-Agent's live-run lock and 10-min Test line — so report, do not run); the skill's post-sync step tells the consumer to run `bash hooks/run-gate.sh` once and treat exit 78 with the marker as the loop; (3) drop "carrying local edits" from the refusal condition: an unmodified template run-gate.sh with `**Gate**: bash hooks/run-gate.sh` is already broken today and the sync is the one moment anyone looks — report it regardless.

## Sentinel predicate MEASURED from plugin source (controller, 2026-09-05)
Writer = the context-mode **MCP server** (`~/.claude/plugins/marketplaces/context-mode/server.bundle.mjs`, `writeRoutingInstructions`), NOT the SessionStart hook (`sessionstart.mjs` only READS CLAUDE.md files to capture rules). Config: `fileName:"CLAUDE.md"`, `globalPath: ~/.claude/CLAUDE.md`, `projectRelativePath:"CLAUDE.md"`. Predicate, verbatim from the bundle: `return a.includes("context-mode") ? null : (write(o, a.trimEnd() + "\n\n" + block))`. So: exact substring `context-mode` anywhere in the file suppresses the append; the block is appended on server startup, so any session with the MCP server registered re-appends. Consumers' recorded mechanism confirmed; §5 fix stands: one template-owned line containing `context-mode` + literal table + six-variant consistency check + same line in the toolkit ROOT CLAUDE.md. Re-check the predicate at each plugin upgrade (the check greps the bundle for `includes("context-mode")` — cheap, and it turns "recorded mechanism" into a measurement each release).

## Server owner answers — batch 4 (normalisation, measured from code) and batch 5 (+ addendum), all accepted
Batch 4: `_sha256` and `_read_file` strip a leading BOM; `_read_file` = `Path.read_text` universal-newline so CRLF/lone CR fold to LF before hashing; `_git_show_file` `text=True` same folding on the base; trailing newline NOT normalised; `_write_file_atomic` writes LF, no BOM. IDENTICAL = identical after BOM strip + EOL fold; post-overwrite the file IS byte-identical → §8's sentence true post-apply, pre-apply comparison is normalised — §7 states both. v3: keep normalisation; add per-file `encoding_drift: [] | subset of ["bom","crlf"]` informational (one byte read, prefix + `\r\n` check, then decode). Trailing-newline differences stay real.
Batch 5: (1) `gate_self_reference` in three places — definition (after addendum): any whitespace token in a `required_keys` value of an audited once file that is template-class by the ownership rules (directly or via `target`), edited or not; `compute_status` reports `gate_self_reference: [{key,path,status}]` (a report tool cannot refuse); `template_apply_file` on such a path refuses before any write naming the key; `template_migrate_manifest` refuses the same way (dry_run reports); documented as DIRECT-only; wrapper fixture (open-brain ARM A) = negative test; `gate_unverified: true` on compute_status and migrate whenever the audited once file declares `**Gate**:`; server never executes it. (2) `region_was_seed`: region body compared normalised with the template's own region body at the base; identical → omitted, `region_was_seed: true`; base unavailable → kept, `null`. (4) test: v2 + lastSyncedVersion/lastSyncedVersionOf + 26 clean + 3 keep-mine → v3 with server-written template_version, both keys in unknown_keys, keep-mine entries template class with templateHash carried as hash. **Server implementation plan written and APPROVED by their user** (`mcp-dev-servers/docs/plans/2026-09-05-v3-ownership-implementation.md`); batches 4–5 being folded in before execution.

## panoscribe (2026-09-05, python, held 86561fe, 27 UP_TO_DATE / 2 PROJECT_CUSTOM / 1 CONFLICT / 3 deviating, read-only) — ACCEPT the model; two release-blocking findings; round complete
**F9 confirmed a THIRD time (RED-B): §7 step 2 diffs against the RAW template while §6 hashes the RENDERED one — internally inconsistent, migration takes the raw side.** Their region-stripped CLAUDE.md vs raw template: 3 hunks (+8/−7): :70 genuine edit; :85 PURE placeholder substitution (never touched); :148 MIXED (four substitutions + a hand-added `bash hooks/run-gate.sh` line + the deleted "Replace placeholders" line). **§8's vacuity control as written CANNOT catch it:** "no region, no edits → header-only" fails on any real tree because every placeholder site diffs; it passes only on a fixture whose template has no placeholders — green there is evidence the fixture is unrepresentative (same shape as the v3.0.3 run-gate defect: check and paired assertion disagreed and read green). *Fix:* render the held-version template with the manifest placeholders before diffing (one line; §6/§7 agree); **§8's vacuity fixture MUST be a tree whose held-version template CONTAINED placeholders, no region, no edits → header-only** — panoscribe's tree is that fixture and they run it against the tag. Header marker gains baseline provenance: `migration-base: <sha> | unavailable; rendered: yes|no`. Emit the "Migrated" section only when hunks remain AFTER rendering (theirs: 1 instead of 3; ZERO if the template takes the run-gate.sh Quick Start line).

**F27 — BLOCKING #2 (new): placeholder VALUE correctness, not coverage.** Their `placeholders.BUILD_COMMAND = "uv run pytest"` while PROJECT_CONTEXT.md `**Build Command**: uv sync --extra dev --extra api` — setup filled BUILD_COMMAND with the TEST command; their CLAUDE.md:88 already reads "Use `uv run pytest` + `uv run pytest`", an instruction file telling agents the wrong build command today. Under v3.1 that re-renders forever and silently reverts their hand-corrected Quick Start. The key audit as specified (missing keys, `{{…}}` values) cannot see a WRONG value. *Fix (server audit + ownership.json):* where a key and a placeholder name the same fact (`**Build Command**` ↔ `BUILD_COMMAND`, TEST/FORMAT/LINT likewise) — declared on the audited rule as `"placeholder_map": {"Build Command":"BUILD_COMMAND", …}` — compare (whitespace-normalised) and report `placeholder_key_divergence: [{key, key_value, placeholder, placeholder_value}]`. "One fact stored twice, mine disagree, nothing looks — the VERSION-line lesson again."

**F28 — variant gap vs F24 (open-brain): reconcile.** panoscribe's Quick Start adds `bash hooks/run-gate.sh` — every consumer with a Gate wants it and the template omits the one command the merge gate requires. open-brain's F24 drops the Quick Start command block to stop placeholder re-injection. *Resolution:* CLAUDE.md's Quick Start keeps NO placeholder-bearing commands (F24) and ONE placeholder-free line — the gate: `bash hooks/run-gate.sh` (F28) — pointing at PROJECT_CONTEXT.md for build/test/format/lint. Both edits vanish under §5a's prediction.

**gate_self_reference — existence proof of the recommended shape:** their `**Gate Command**` = `bash preflight.sh && uv run ruff format --check . && uv run ruff check . && uv run pytest --cov=…` names nothing under hooks/; `preflight.sh` sits at the REPO ROOT, not manifest-tracked, not a template file; all 15 hooks/ entries unmodified; its own header (written before this round) states open-brain's argument: "it lives here rather than inside hooks/run-gate.sh — that file is synced verbatim and every local edit forces a hand-merge." *Guarantee to state and TEST in §6:* ownership.json is first-match over `**` globs, so a future template-class `*.sh` rule would clobber `preflight.sh`; "a path absent from `files` is project" protects it → a consistency-check row (toolkit: no ownership rule pattern matches a path outside the template tree — e.g. every pattern must match ≥1 template-tree file and none is a bare root `*.ext`) + the server test from batch 2 (6). Also: their deprecated spelling `**Gate Command**` is live (F2).

**Q5 (LOCAL_EDITED evidence): with the baseline fixed, backup dir + inline `local_diff` IS enough; naming hunks adds nothing (`.pre-sync` is the full pre-image). Refusing without `backup_dir` is the load-bearing part.** Don't spend design budget on the reporting layer to compensate for a wrong baseline.
**CLAUDE.local.md byte-identical (211 lines): FOURTH consumer.** Migration would CREATE the duplicate-load (stale project copy + rules/mcp.md + rules/sqlite.md); server can prove it from data it holds (v2 local_hash == template_hash) → report + OFFER deletion (never unasked); a diverged copy stays and is told which sections are now duplicated. The open-brain section (dropped as server-injected) would survive only by accident inside the stale copy.
**Rules:** one, `.claude/rules/python.md`, unmodified, `{lang}` → template, zero loss; project.md always-on is right ("a file the consumer writes prose in shouldn't silently stop loading because a glob missed").
**Fenced hunks: YES with the baseline fix**; §7 step 3's rationale "the strongest paragraph in the spec".
**Sentinel — nested layouts:** `grep -c '^# context-mode' CLAUDE.md` → 0 in their repo but the PARENT `G:/git/CLAUDE.md` → 1; the plugin appends to the nearest CLAUDE.md for a nested layout, so the step-9 guard's premise fails for nested repos (harmless for them; parent never staged). Note in the skill: the guard checks the file the plugin actually targets (the session's project dir), which may be a parent.
**SEPARATE (v3.0.x, third independent report of git-cmd.sh:934-938):** five arms on protected main: A `git -C <abs> merge-base --is-ancestor A B` → 2 (want 0); B same without -C → 0 (isolates -C); C `git -C <abs> merge f/x` → 2 (want 2, gate live); D `git -C <abs> merge-tree A B` → 2 (want 0); E `git -C <abs> checkout feature/merge-fix` → 0 but CONFOUNDED (branch-change logic at :834-837) — not a clean control. Scope = hyphenated `merge-*` plumbing family. → warm-up coder: add the merge-tree row; do not use E.
**Sync status:** panoscribe holds v3.0.3; v3.0.4 verified, unsynced, user's call; their session loaded skill body v3.0.3 while installed is v3.0.4 → step-1 marker assertion STOPS (remedy: restart) — the guard working on a real case.
## Server owner answers to batch 1 (MM-Agent findings) — written into their review §5
(a) Key audit ACCEPTED, simplified: the audited rule carries `required_keys` (list) and `deprecated_keys` (map old→replacement); NO `optional_keys` — every template key not in required_keys is optional, so the lists cannot disagree with the template. Report: `missing_required`, `optional_absent`, `placeholder_keys`, `deprecated_keys`; project-only keys ignored; names compared without the `**…**:` decoration. (b) Orphans ACCEPTED, no opt-in, no hard-coded dirs: orphans = files on disk matching a template-class rule, in no manifest entry, not shipped by the template (shipped ones are `new_template_files`). A consumer silences a path with an explicit `{"pattern":".claude/agents/local-*.md","ownership":"project"}` rule placed before the broad one. Informational. §4 narrows to "never reads or writes; lists orphans under template-class patterns". (c) `redundant_project_file` ACCEPTED, migration-only: every entry dropped because its path is now project-class (or unclassified) and still on disk is compared with `_apply_placeholders(git show <template_commit>:<path>)`; identical → report with delete suggestion, never deletes; base unavailable → skipped under `migration_base_unavailable`; not recomputed on later syncs. (d) `transitional_duplicates` skill-side from `local_diff`. **CONSEQUENCE that revises F3:** settings.json is `template` class and there is NO merge class by design — a deliberate local addition is `LOCAL_EDITED` on every sync and overwritten every time with the diff saved. So: MM-Agent's six `Read(.env*)` rules must be RETIRED before their first v3.1 sync (the template cannot ship them), or they re-apply from `.pre-sync` after each sync — §5a does NOT gain a third category; the transitional case becomes a pre-sync task named in the migration note; and the SubagentStop matcher extension (F3a) MUST be solved in v3.1 (derive from `.claude/agents/*.md` or a declared key), because otherwise MM-Agent's two project agents lose their contract hook on the first v3.1 sync. → F3a moves from "decide in the plan" to a v3.1 REQUIREMENT with MM-Agent as verifier.

**MM-Agent round CLOSED.** Their pre-v3.1 steps recorded in their PROJECT_STATE: verify the shipped SubagentStop matcher derivation against mm-runner/finisher before syncing — **constraint: the derivation must include agents that exist ONLY in the consumer tree** (the hook reads `.claude/agents/*.md` of the PROJECT at runtime, not a template-time list); Read(.env*) retirement is their user's call (date put in front of them); permanent orphans silenceable per path accepted.

## MM-Agent — §2 row 5 answer (why a consumer holds on a version, measured this week, in weight order)
1. Session-boundary coupling: changed agents/skills need a restart; a restart mid-milestone costs the PO context and any in-flight coder pipeline → syncs are scheduled at milestone boundaries (v3.0.4 held since ~09:00 for a running T3 pipeline). 2. Live-run lock (WinError 5; runs last 5+ h; the sync waits, never the reverse). 3. Wall-clock cost of VERIFICATION, not the hand-merge: 2–3 h PO time (three backup sets, 6a/6d/6b, placeholder sweep, probes, plus 10-min pre-commit Test per doc commit); the two keep-mine splices took <15 min. Ownership removes the splice, not the rounds. 4. Content-gated: docs/text releases carry no enforcement change so holding loses nothing; hook/matcher releases were taken within a day. NOT distrust. *Spec consequences (§8/§11 and the skill):* the sync report must say `restart_required: yes|no` from whether agents/skills changed; the skill's verification rounds must shrink as the merge disappears (ownership makes 2b's control, step 4, 6b applied-set moot — fold-in §3.3.17); a release should classify itself docs-only vs enforcement in the CHANGELOG so consumers can content-gate deliberately.

## penumbra (2026-09-05, main 98cb41a, manifest v3.0.3 @ 86561fe, python, 32 tracked, read-only) — GREEN with four REDs that must land before the tag

**F8 — RED-A: §7 step 2's base path is wrong.** `git show <template_commit>:CLAUDE.md` names the toolkit-ROOT CLAUDE.md; it must be `templates/<variant>/CLAUDE.md`. *Spec fix, verbatim.*

**F9 — RED-B: the migration base must be PLACEHOLDER-REPLACED before diffing.** The v3.0.3 python template carries four placeholders (`{{BUILD_COMMAND}} {{FORMAT_COMMAND}} {{LINT_COMMAND}} {{TEST_COMMAND}}`); an unreplaced base yields four spurious "local edit" hunks for EVERY consumer. §6 already says hashes are post-replacement; §7 step 2 must say the same (server: `_apply_placeholders` on the base before diffing).

**F10 — RED-C: the context-mode sentinel.** The context-mode plugin's SessionStart writer appends its routing block to any project CLAUDE.md lacking the substring `context-mode` (measured: penumbra's region ends with an HTML comment containing it; the toolkit's own root CLAUDE.md lacks it and shows ` M CLAUDE.md` every session — verified here). Today template line ~161 satisfies it; §5 removes the markers and presumably that line. If the ≤6 KB v3.1 template CLAUDE.md has no `context-mode` substring, every context-mode consumer gets: writer appends → LOCAL_EDITED every sync → overwritten → appended again. *Spec fix (§5 + diet plan Task 0 literal table):* the template CLAUDE.md keeps ONE line containing `context-mode` (the existing "plugin routing blocks belong in `.claude/rules/project.md`" pointer, reworded for the new home); add it to the literal-preservation table with the writer as the reason; consistency check asserts the substring in all six.

**F11 — VERIFICATION_PLAYBOOK.md is a finding against §5a and absent from the §4 class table.** Shipped by the template (`templates/python/VERIFICATION_PLAYBOOK.md`), appended by the project (nine incident rules from corrections 66–84); no hook reads it. Neither variant gap nor per-project value — an append-to record like PROJECT_STATE.md. ownership.json would report it `unclassified_template_files` (the guard works). *Decision needed now:* class `once`, or split `template` playbook + `once` companion so the four always-on template rules keep flowing. Controller lean: `once` (one file, the project owns its incident rules; template rule updates arrive via CHANGELOG, not overwrite) — put to the round.

**F12 — hooks/enforce-delegation.sh keep-mine (+14 lines at :185, three PO write-surface regexes `docs/`, `tools/`, `tests/`) → per-project value.** Key it: `**PO write surface**:` in PROJECT_CONTEXT.md (extra path prefixes; `none` = template default), read by the hook — same mechanism as `**Gate**:`. Then the hook returns to `template`. *Toolkit hook item; add to fold-in §3.1; `optional_absent` in the key audit.*

**F13 — glob classification is TEMPLATE-tree only; consumer files matching a pattern but absent from the manifest are `project`.** Their `.claude/agents/cpp-coder.md`, `science-reviewer.md` match `.claude/agents/*.md` = template. §6/§7 must say ownership.json patterns classify template-tree paths (and `new_template_files`); a consumer-tree file matching the pattern but not in the manifest is `project`: never diffed or swept by 6d — but LISTED as an orphan under F7. One sentence.

**F14 — third migration fixture: REGION-ONLY.** penumbra's CLAUDE.md diff vs `templates/python/CLAUDE.md` @ 86561fe (placeholders replaced): one hunk, entirely inside the markers (51 region lines). project.md must carry the 51 lines verbatim and NO "Migrated" heading. Add to §8 beside "region + out-of-region" and the "neither" vacuity control.

**F15 — CLAUDE.local.md byte-identical (locallyModified:false); same F6 ask, sharpened:** compare against the held-version `templateHash` the v2 manifest already holds BEFORE dropping the entry; report "identical to template at <version>; superseded by `.claude/rules/mcp.md`; safe to delete". Report only.

**F16 — `template_version` derivation must be stated in §6:** the nearest reachable tag whose tree over the tracked template paths (hooks scripts templates user-level-reference) equals the tree at `template_commit` (B1 case 2) — never `git describe` (its suffix is what the user rejected). No such tag → `template_version: null` + warning `untagged_template_tree` at finalize (skill §1b already STOPs unless overridden). Their case: template_commit 5ce2d14 → v3.0.3, correct.

**F17 — `template_commit` durability:** a SHA taken from a branch tip that is later squash-merged and deleted becomes unreachable after gc; the migration must fall back to `template_version`'s tag commit (tree-identical by construction) BEFORE emitting `migration_base_unavailable`.

**F18 — item 11 (tracked-only tree hashing): SHIP in v3.1, not a design pass.** Would have prevented v3.0.3 item 8 (in-repo commit-message file) and the untracked-review-file case; would not prevent mutation-batched-with-commit (last-precommit.json's discriminator, already shipped). `add -A` never protected against a contaminating untracked file, it only forced a second run in the same tree. Shape: copy the real index (keeps staged new files), `git add -u`, `write-tree`. Three-arm fixture: untracked unignored file present → artifact tree == HEAD^{tree} (match); unstaged edit to a tracked file → mismatch (control); `git add new.py` → gate → commit → match. Finding-59: no opinion. *Move fold-in §3.2.11 (tracked-only half) to §3.1 as a v3.1 hook item; run-gate.sh, no lib.*

**Item confirmations:** item 1 as one lib item + a THIRD 0-control `git -C <path> log -- docs/merge.md` (verb word in an operand) → send to the warm-up coder. Item 4 exact sentences: compound — "refused: <verb> evaluated on branch 'main' — the 'git checkout -b <X>' earlier in this call has not run when this hook fires; split the call: checkout first, then <verb> alone."; pipe — replace "earlier clause" with "the pipe's later stage (`tail`) is not inert". Item 19 alias confirmed. Item 16/§9 load-bearing confirmed. Item 15 reworded: "prediction commit made alone in its own call, on the branch the sync will use, with last-precommit.json tree == HEAD tree" (if item 11 ships, `add -A` is no longer the cause). **Nice-to-have (server):** `template_migrate_manifest(..., dry_run=true)` returning project.md content without writing, for §8's read-only pre-release check on the live tree. **§2 row 5:** yes, the §7 shape makes penumbra sync on release day given RED-A/B/C, the playbook class and the write-surface key.

### Post-round addenda

Copied verbatim from `round-addenda.md` (items that arrived after the amendment coder
started reading the ledger; folded into §3.1/§3.3 above and into the ownership spec §8).

---

# Post-round addenda (arrived after the amendment coder started reading the ledger; fold into the next docs commit or the toolkit implementation plan)

## Server batch 6 — all accepted (their review §10; round COMPLETE on the server side)
(1) Base always placeholder-rendered before diffing; header `<!-- template-sync: project-owned; migrated from CLAUDE.md at v3.1.0; migration-base: <sha>|unavailable; rendered: yes|no -->` (`no` only when the base is unavailable); "Migrated" section only when hunks remain after rendering; server vacuity fixture uses a held template that CONTAINS `{{NAME}}` with the consumer holding the rendered text. (2) `placeholder_key_divergence` accepted: rule declares `"placeholder_map"`; compare each present key's value (exact/qualified/alias lookup) with the manifest placeholder value, whitespace-normalised; report `[{key, key_value, placeholder, placeholder_value}]`; a placeholder missing from the manifest → `placeholder_value: null`; never writes. (3) Test row: root-level consumer file (`preflight.sh`) matching a template-class `*.sh` rule with no manifest entry — never in files, never diffed/applied, appears only under `orphans`. Review file §1–§10 = full server contract; implementation plan carries every item; execution starts when they receive the amendment sha (user-approved).

## penumbra — FIRST CONSUMER ON v3.0.4 (2026-09-05): v3.0.3 → v3.0.4, merged dbb5789 (PR #152), prediction committed first (df3fd10), every predicted row held
GREEN: §1b case 2 fired exactly as written (checkout 5d7bbb1 on main, no exact-match tag, nearest ancestor v3.0.4, tracked-path diff clean → manifest lastSynced 1450034 / lastSyncedVersion v3.0.4 / lastSyncedVersionOf 1450034; post-stamp compute_status clean 28/4/0/0). 7b stale-label backstop observed LIVE: finalize wrote lastSynced=5d7bbb1 and PRESERVED the v3.0.3 stamps — the "Of ≠ lastSynced" window the skill predicts; the stamp cleared it. Status matched the tag diff (auto-update 2, conflict 1, no model bump); step-4 heading-first placement check GREEN on the auto-merged output (added line under `## Build System` both sides) — a real GREEN beside yutraffic's RED. deny-secret-reads new message verified on the installed hook; probe 2/0/0 on the feature branch. 6b 13/13; sweep 1/1/1; drift 28/28. 2b both-arm control PASS, .gitignore restored byte-identical. Merge-gate TREE arm carried the merge (artifact sha = parent, tree = HEAD^{tree}; message file outside the repo, commit alone, no untracked files) → clean evidence for item 11's "tracked-only would not change the passing case".

**Findings for v3.1 (skill text):**
- **A.** SKILL.md §1b case 2 says "rewrite lastSynced to the TAG COMMIT before 7b (or have finalize record it)" — the second arm does not exist: `template_finalize_sync` takes no commit argument and records the checkout HEAD unconditionally. Until server 0.3.0 writes `template_commit`/`template_version`, the rewrite is client-side only → drop the parenthetical or mark it "server 0.3.0".
- **B.** `last-precommit.json` overwritten by every Bash call — measured again; fold-in §3.1 item 2 stands (write only when a commit segment exists).
- **C.** Report-template: the `Toolkit:` line between finalize and 7b prints `v3.0.3 (5d7bbb1) STALE LABEL` — correct, but a crash in that window leaves a manifest naming the wrong release → 7b states the window exists and the stamp closes it.
- **D.** Step 3 probe note: `no-push-main` keys on the PUSH TARGET, not the current branch — the probe now runs pre-merge on a feature branch by construction; one sentence.
Line-937 refusal ships unfixed in v3.0.4 as expected; workaround in their memory.

## yutraffic — SECOND CONSUMER ON v3.0.4 (2026-09-05): 86561fe → 1450034, merged dc012d3 (PR #245), CI green; auto_update 0 / conflict 0 post-finalize; sweep clean with live control; drift 28/28; hooks 13/15/15/0; probe 2 0 0 node
- **§1b case 2 fired and mattered:** HEAD 5d7bbb1 (one docs commit past the tag, tracked tree identical) → without 1b they would have stamped `v3.0.4-1-g5d7bbb1`, an unreleased sha under a label nobody can name; recorded 1450034 / v3.0.4. "Case 2 is not a corner case: it fires whenever a release is followed by any docs commit, which is your normal cadence."
- **GAP 1 (skill):** the bash→python `/tmp` warning is in the intro, 2b and 7b but NOT at 6b — where it bit twice (`/g/git/…/template-manifest.json` then `/tmp/all.txt`, the step's own grep pipeline output — literally the intro's example). 6b should say: derive the referenced set inside ONE process, never stage it through /tmp.
- **GAP 2 (skill):** a "no unfilled placeholder in my spliced output" check false-positives on PROJECT_CONTEXT.md — the protected-branches comment legitimately contains a literal `{{…}}` while explaining fallback. Step 4 should send the reader to step 7's arm-1 pattern (`- **Key**: value`), not a bare `{{` test: "this file documents placeholders in prose".
- **last-precommit.json overwrite — 2nd consumer, concrete:** after the commit the artifact read `no-commit-segment, rc -1, tree "", cmd_len 545` = their own inspection command. "Inspecting the artifact is itself what destroys it." Fix (write only on a commit segment) confirmed as the one that preserves the row. → fold-in §3.1.2, now three consumers.
- **Artifact arms (bears on item 11):** `last-pass.json` sha = b2ef758 (PRE-commit HEAD; PreToolUse runs before the commit exists), tree = f839c5e2 = resulting HEAD^{tree}. On a Gate-only repo's first commit on a new branch the SHA arm can NEVER match; the TREE arm is the only thing making the merge gate pass. The v3.0.4 run-gate comment frames `add -A` untracked exposure as secondary; for this shape the tree arm is load-bearing → item 11 (tracked-only hashing) is not optional polish; reword the run-gate header when it ships.
- **Environmental (their autoMode):** `git pull --ff-only` on main denied by the classifier, not a hook; `git checkout main` had already reverted on-disk hooks to v3.0.3 → between merge and fast-forward the tree runs the PRE-sync enforcement layer; harmless for a text release, not for a security one. Step 9's note ("local main carries the pre-sync hooks until it fast-forwards") should be promoted OUT of the code block — a consumer who cannot complete the pull sits in that window indefinitely.
- Deny text verified live in situ; verdicts unchanged (2/2/0/0).

# Read-back corrections on 247ef65 (2026-09-05) — three, all measured; amendment #3

**RB1 — open-brain: `gate_self_reference` tokenising must normalise decoration, or it misses the tree the finding came from.** Raw bytes of their PROJECT_CONTEXT.md:22: `- **Gate**: \`bash hooks/run-gate.sh\``. Whitespace tokens of the raw value are `` `bash `` and `` hooks/run-gate.sh` `` — both carry a backtick, so a token-wise path match against template-class paths does NOT match `hooks/run-gate.sh`; the static check stays silent on open-brain and apply proceeds to overwrite. A backticked value is VALID today: run-gate.sh's extractor strips surrounding backticks from the whole value (`s/^\`//;s/\`$//` at :104) before eval. The template ships the bare form (`- **Gate**: {{GATE_COMMAND}}`), so the decoration is consumer/setup-authored and cannot be exercised from the template side. *Spec fix (§7):* "tokens are compared after the same backtick/whitespace normalisation `run-gate.sh` applies to the field value — strip surrounding backticks from the value and from each token, normalise a leading `./` and a leading `bash `/`sh ` — a backticked value is valid and must match"; fixture row with a backticked value (decoration ≠ indirection: they fail differently). *§5a:* add "inline the steps, do not shim — a thin wrapper re-creates the loop (§7)". Everything else in §5/§7 verified line by line by open-brain (redundant_project_file compared BEFORE the entry is dropped; region_was_seed three states; rendered base reproduces their 3-hunk count; unknown_keys; gate_unverified split).

**RB2 — panoscribe: §8 records them in the wrong fixture shape, and one number was theirs.** Their region is 1692 B / 6 lines of genuine content (Green-CI gate definition, run-verification rule, artifact location, `uv sync` bootstrap note, compact rule) — `region_was_seed: false`, written verbatim. After RENDERING, 2 hunks survive (not 1 as they first said — a MIXED hunk survives rendering by definition): :70 reworded Required-Skills paragraph; :148 the Quick Start block (substitutions vanish; the added `bash hooks/run-gate.sh` line, the deleted "Replace placeholders above…" line and the comment-column respacing remain). Zero is not reachable from the Quick Start change alone; after the template takes the gate line ONE hunk remains (:70), dropped by choice. *Correct §8 shape for panoscribe:* "region + out-of-region edits on a held template that CONTAINED placeholders, consumer holds rendered text" — the only fixture where a MIXED hunk is real rather than constructed. *The vacuity control (no region, no edits, placeholder template) is NOT panoscribe:* candidate = yutraffic (CLAUDE.md byte-identical at held version, region placeholder-only → likely the seed) — confirm with yutraffic that rust-tauri's template CLAUDE.md contains placeholders; the server's synthetic `{{NAME}}` fixture (batch 6) satisfies the property regardless. Footnote: substitution does not re-align the trailing comment column (`uv run pytest               # Build the project`), so a consumer who tidied it carries a permanent whitespace-only hunk — not a spec change; a note for readers of hunks. `placeholder_key_divergence` on their tree: exactly one row (BUILD_COMMAND).

**RB3 — yutraffic: §5's qualified matching applied to `required_keys` produces a FALSE GREEN.** `hooks/pre-commit-test.sh:263` reads `grep -E "${GC_KEY_PRE}\*\*Test( Command)?\*\*:"` — a qualified form matches NOTHING; their PROJECT_CONTEXT.md holds `**Test (frontend unit)**`, `**Test (frontend e2e)**`, `**Test (backend)**` and zero bare `**Test**:`. The audit as amended would report `Test` present (`matched_as: Test (frontend unit)`) while the hook sees no Test field and falls back to the Gate — "a false alarm gets ignored, a false green gets believed"; same shape as b3 (audit matcher vs hook matcher disagreeing about one file). *Spec fix (§5/§7):* qualified/alias matching applies to OPTIONAL/documentation keys only; for `required_keys` the comparison is the hook's own regex (exact `**Key**:` or its deprecated spelling), and a qualified-only holding is its own state `qualified_only` ("you hold `Test (frontend unit)`; the hook matches `**Test**:`/`**Test Command**:` exactly and will not read it"). *Second decision:* `Test` is NOT required anywhere — the documented Gate-only shape (rust-tauri ships no `**Test**:`; pre-commit-test falls back to the Gate, which mints the artifact) is a default, so `Test` is optional with `optional_absent` reading "default applies — pre-commit-test falls back to the Gate". `required_keys` = `Protected branches`, `Gate`. (Alternative rejected: per-variant required lists — more state for the same answer.) `template_default_changed` correctly stays silent on their `main` vs `main master` (template held `main master` at both versions; plain `value`/`template_value` surfaces the deliberate narrowing).

# Read-back addenda 2 (after amendment #3 was dispatched)

## RB4 — yutraffic: the §8 vacuity row splits in two, and no real consumer supplies the reversal half
Measured at 1450034: their PROJECT-CUSTOM region body = the 73-byte seed exactly (condition B holds); the rust-tauri template CLAUDE.md contains `{{` ZERO times (condition A fails) — `templates/general/CLAUDE.md` carries 3. rust-tauri hardcodes `npm install` / `npm run tauri dev` / `npm test`, so their byte-identical CLAUDE.md (12583 B both sides) is trivially identical and exercises no placeholder reversal in step 2.
*§8 row split:* (a) **OUTCOME** — a real tree with an empty (seed) region and no out-of-region edits must yield a header-only project.md with no hunk section: **yutraffic confirms on a live tree.** (b) **REVERSAL-UNDER-PLACEHOLDERS** — held-version template carries `{{…}}`, project holds the filled values, step 2 must still produce zero hunks (the case that catches a reversal bug): **no consumer in the round has this shape** (MM-Agent: region content is theirs; open-brain: seed region + 3 edits; panoscribe: region + edits; penumbra: region-only) → carried by the server's synthetic `{{NAME}}` fixture ALONE, stated as such in §8 rather than attributed to a consumer. Any future `general`/`python`/`java`/`dotnet`/`dotnet-maui` consumer with a seed region and no edits becomes the live instance (one grep to check).

## Server batch 7 — accepted (their review §11); execution at Task 4 of 11 (rules, gate, load done; server reports 0.3.0 on the branch)
(1) `gate_self_reference` tokenises after the hook's normalisation: surrounding backticks stripped from the whole value and each token, leading `./` dropped, leading `bash`/`sh` token ignored; the key-line regex tolerates a list marker (`- **Gate**: …`, open-brain's :22 shape); test row with the backticked list-item value. (2) `required_keys` = hook's exact match only (`**Key**:` or deprecated spelling); qualified/alias-only holdings → `qualified_only: [{key, held_as, note}]`, not counted as required; qualified/alias stays for optional keys. (3) fixture `required_keys` = Protected branches, Gate; `Test` optional; the "default applies — pre-commit-test falls back to the Gate" sentence is the SKILL's to print (no server text). → skill item.

## RB5 — yutraffic: "held version" is only as current as the WORKING TREE (window CLOSED on their side 2026-09-05: user ran the fast-forward; HEAD dc012d3, hashes 31362b10cfaf/6e5829b48613, manifest v3.0.4, probes 2 0 0 / 2 2 0 0, deny text in place — general note stands)
Their measurement read the manifest as `lastSynced: 86561fe / v3.0.3` although the v3.0.4 manifest is in merged commit dc012d3 on the remote: `git checkout main` reverted the working tree and `git pull --ff-only` is blocked by their environment's classifier until their user acts. Between merge and fast-forward a consumer reports the PREVIOUS release in good faith. *Note for §1b / docs/verification.md / the skill's step 9:* "held version" = the manifest in the working tree; a consumer measuring between merge and fast-forward must say which sha their tree is at (and the pre-sync hooks run in that window — already promoted out of step 9's code block).

# panoscribe — merged v3.0.4 (c1c9eb7, PR #139; CI green; post-finalize 0/0; drift 28/28; six hashes byte-identical; local gate 674 passed, 98.57 % coverage) — third MERGED consumer (open-brain committed, unmerged)
- §1b case 2 fired (HEAD 5d7bbb1 one docs commit past the tag, tracked diff clean → 1450034 / v3.0.4); both 7b control arms passed with the property-not-position fixture selection (untagged 5d7bbb1 → `v3.0.4-1-g5d7bbb1`, carries the suffix). Fourth live confirmation of case 2.
- The one CONFLICT was the misplacement shape (PROJECT_CONTEXT.md, one added line); spliced; placement asserted — same heading `## Build System`, same distance (8) both sides, rest byte-identical; written file byte-compared to the built file. "The check is cheap to run even when it passes."
- **B5 point-of-use (skill):** the manifest's `templateRepo` is MSYS-form (`/g/git/...`); python's `git show` died `fatal: cannot change to '/g/git/claude-code-toolkit'`. One line where the path is consumed: "the manifest's templateRepo is MSYS-form; convert before handing it to python."
- **B2 load-bearing every sync:** their `.gitignore` (949 B) ends without a newline, last rule `!tests/fixtures/e2e/*.wav`; the leading-newline append left it byte-identical after restore, both arms passed.
- **NEW DEFECT (v3.0.x, fail-closed, → v3.1 Phase 2, lib):** `2>&1` alone converts an INERT clause into a "mover" in gate-before-merge.sh. Probes on protected main: F `echo hi; git push origin --delete zz` → 0; D `echo hi 2>&1; git push …` → 2; G `true; …` → 0; H `true 2>&1; …` → 2; I `echo hi > out.txt; …` → 2 (correct, a real write); E push alone → 0 (control). `2>&1` is an fd DUPLICATION — creates no file, cannot write `.git/config` under any reading; arm I shows the real-write case is separately caught, so narrowing the redirection rule to redirections with a FILE operand (`>`, `>>`, `&>`, `>|`, `<`) loses nothing. Cost is real (`cmd 2>&1` is the reflexive output capture) and the printed remedy (split the calls) never mentions the redirection. Fixture = arms D/F/G/H/I/E. *Plan: Phase 2 new Task 2.6b (git-cmd.sh clause classifier; matrix already mandatory).*
- **Diagnostic names a non-operative property (panoscribe self-correction):** their block named `git branch -d … 2>&1` and attributed the mover class to the redirection; arm B (`git branch -d zz; git push …`, no redirection) is ALSO exit 2 — the ref-changing clause was independently sufficient, the VERDICT was right, the stated reason was not the operative one. *Rule for the discriminator text (Task 2.6):* when several clause properties each suffice, name the one that decided, or name them all.
- **Step 7 "build applied_files programmatically, never hand-assemble" is NOT achievable as written:** `template_finalize_sync` takes `applied_files` as an inline JSON STRING and per-file results arrive as transcript text — every path from tool result to parameter passes through retyping. Their backstop: the post-finalize self-check (clean). *Two fixes:* server accepts a FILE PATH for `applied_files` (the way write_db does) — batch 9; skill names the post-finalize self-check as the enforcement until then.
- F27 divergence (BUILD_COMMAND holds the test command) deliberately left — their user's decision; expected first `placeholder_key_divergence` row.

# Read-back addenda 3 — open-brain's v3.0.4 sync (third consumer on v3.0.4, 2026-09-05; commit 774787a on chore/sync-template-v3.0.4, not yet merged — their gate needs their user's go-ahead; post-finalize auto_update 0 / conflict 0; gates 2/0/0; drift 28/28)

1. **§1b case 2 measured both ways:** finalize wrote `lastSynced: 5d7bbb1` (HEAD, one docs commit past the tag); 7b control arm 2 printed the counterfactual label `v3.0.4-1-g5d7bbb1`; after the rewrite 1450034 / v3.0.4 / 1450034. open-brain: "the server has no tag knowledge and never will, so the client-side rewrite is permanent work" — CORRECTION for the record: under server 0.3.0 the server derives `template_version` at finalize (batch 2 (3): `git tag --merged <template_commit>`, tree-equal over `tracked_paths`), so the client rewrite is transitional until 0.3.0 — penumbra's finding A already marks the 1b parenthetical "server 0.3.0". ~~§1b text: "client-side until server 0.3.0; server-derived after".~~ **CORRECTED BY open-brain (measured on server 0.3.0, in-progress build, v2 manifest): `template_load_manifest` returned `server_version: "0.3.0"` BEFORE any write; `template_finalize_sync` still returned `last_synced: 5d7bbb1` (HEAD, not the tag) on a checkout whose tracked-path tree IS byte-identical to the tag; post-finalize manifest carried no `template_version`/`template_commit`.** The server owner's design keeps the v2 path byte-stable (v3 classification + migration in a new module), so the derivation applies only to v3 manifests — the release number alone does not predict the behaviour, and "server-derived after 0.3.0" would make a v2-manifest consumer on 0.3.0 skip the rewrite and stamp `v3.0.4-1-g5d7bbb1`, the exact label 7b exists to prevent. *§1b wording keyed on an OBSERVABLE (open-brain):* "client-side while `template_load_manifest` returns `manifest_version < 3` or the post-finalize manifest carries no `template_commit`; server-derived once finalize writes it — after finalize, if `template_commit` is present trust it and skip the rewrite; if absent, rewrite." Reads the same on every server × manifest combination and cannot go stale on a release number. (Penumbra's finding A was measured on a v2 manifest against 0.2.1 — same condition, no disagreement on behaviour.) **SERVER CONFIRMED both:** (a) the v2 path in 0.3.0 is the 0.2.1 code verbatim — finalize writes `lastSynced` = `git rev-parse --short HEAD`, `version: 2`, never `template_commit`/`template_version`, never the tag derivation; (b) the v3 path (`manifest_version == 3`, dispatched before any v2 code) writes BOTH at finalize — `template_commit` = full HEAD sha, `template_version` = nearest tag reachable (`git tag --merged <sha> --sort=-v:refname`) with equal tree ids over `tracked_paths`, else null + `untagged_template_tree` (`template_repo_not_git` when not a git repo). The observable "manifest carries `template_commit` after finalize" separates the paths exactly, and holds for a manifest produced by `template_migrate_manifest`. → §1b and plan Task 4.1 step 1 use that sentence.
2. **7b stale-label third case observed LIVE on the tree the backstop was written for:** right after finalize, `lastSynced 5d7bbb1 / lastSyncedVersion v3.0.3 / lastSyncedVersionOf 86561fe` — a label-only reader would be told v3.0.3 about a v3.0.4 tree; the companion key made it detectable; unconditional recompute cleared it in one write. Confirms the source read (finalize PRESERVES; a stale label always comes from an older 7b, never the server).
3. **Per-file unknown manifest keys — POLICY GAP.** Their PROJECT_CONTEXT.md entry carried a hand-added `"reason": "Project-specific config"`; `template_apply_file(source="skip")` returned an entry WITHOUT it; step 7's "build applied_files programmatically, never hand-assemble" would have let finalize drop it silently. §7 step 5 drops the NAMED superseded v2 per-file fields; `reason` is not on that list, so today it is an unknown per-file key with no stated policy. *Decision to put to the server:* per-file unknown keys are PRESERVED and reported (`unknown_file_keys: [{path, keys}]`), matching the top-level policy — "silence is the problem, not the policy"; the named superseded fields are the only drops. Skill: step 7 may hand-assemble nothing, so preservation must be the server's. **Server batch 8 ACCEPTED (their review §12):** preserved at finalize from the on-disk entry (apply keeps returning the two-field entry; finalize_v3 merges every existing key that is neither a v3 field nor a named superseded v2 field) and in `template_migrate_manifest` from the v2 entry; both responses carry `unknown_file_keys: [{path, keys}]`; `reason` survives by policy. Server execution: Tasks 1–10 of 11 done, 162 tests green; 2b23958 cited in review file and plan.
4. **6b /tmp pointer — THIRD consumer** (yutraffic GAP 1, open-brain ×3 over the cycle): `/tmp/ref-hooks.txt` written from bash, `FileNotFoundError` from python. Plan Phase 4 Task 4.1 step 6 already: derive the referenced set inside one process, never via /tmp; the sentence sits beside the collector.

Verification notes recorded: deny-secret-reads v3.0.4 delta read line by line — `dsr_is_secret`, `dsr_paths_in`, reader-verb detection untouched, message/provenance only (checkable claim, held); PROJECT_CONTEXT.md `has_conflicts:true` → accept-merged disqualified; placement check GREEN on the splice (`## Commands`, delta 8) and RED on a relocated control (`## Paths`, delta 2) — a second live GREEN/RED pair for the b3 fixture set; 2b both arms green, .gitignore byte-identical, empty sets reported by KIND (b structural, c correct); sweep 1/1/1 + planted `- **Zz Control**: {{ZZ_CONTROL}}` → rc 0 (sweep still sees one); class-3 `-C` resolver refused `git -C $T diff` on an unexpanded variable — correct, clear, one retry, "prevented nothing legitimate" (first real-world hit outside a fixture; the read-only-verb narrowing stays a v3.1 item, now with a measured cost: one retry).

# Addenda 4 — after docs/v31-specs 34f2145 (fold into the next docs commit or at plan execution, Phase 4 Task 4.1)

## panoscribe — F27 FIXED (4b58d6b, PR #140, CI green): `placeholders.BUILD_COMMAND` "uv run pytest" → "uv sync --extra dev --extra api"; CLAUDE.md:88 corrected in the same commit; TEST_COMMAND unchanged (was always right). **Fixture expectation update:** panoscribe's tree no longer yields a `placeholder_key_divergence` row; the F27 case remains in the ledger as the measured origin, with no live consumer instance.

**Skill mechanic 1 — a placeholder VALUE change moves the recorded template hash of every tracked file whose template renders the token.** Measured: CLAUDE.md templateHash 189b5cac → e2d0d0f7, templateRawHash unchanged 2b48f928 (the pre/post-substitution split doing its job); PROJECT_CONTEXT.md likewise. Under v3.0.x both had to be re-registered with `template_apply_file(source="skip")` against the new rendering, or they classify CONFLICT on the NEXT sync for a change nobody made — a self-inflicted conflict arriving when the cause is out of view. *Skill (v3.0.x text, and the v3.1 audit follow-up):* "after changing a placeholder value, re-register every file that renders that token". *Under v3.1 ownership:* template-class files rendering the token read `TEMPLATE_UPDATED` on the next sync (current rendered template ≠ stored hash) and are overwritten with the new rendering — the correct outcome, automatic; `once` files are never diffed; so the v3.1 sentence is "after fixing a `placeholder_key_divergence`, expect `TEMPLATE_UPDATED` on every template-class file that renders the token; the sync applies the new rendering" — Phase 4 Task 4.1 step 5.

**Skill mechanic 2 — `template_finalize_sync` resets `lastSynced` to the checkout HEAD on EVERY call (v2 path).** Their fix needed a finalize of its own (two skip re-registrations) and it silently reverted lastSynced 1450034 → 5d7bbb1; caught by re-running 7b by habit. *Skill (v2 path):* §7b's operative rule is "after EVERY finalize call in the session, whatever prompted it", not once at the end of a sync. Under v3 the observable rule already covers it (finalize writes `template_commit`; if absent, rewrite).

**Squash-merged sync branches are invisible to ancestry-based cleanup.** `origin/chore/sync-template-v303` survived a release because its tip (6cea0ad) is not an ancestor of main after a squash; content settled it (`diff 6cea0ad 0381672` empty). *Skill step 9 / `--delete-branch` note:* after a squash merge, `git branch --merged` and `merge-base --is-ancestor` will not recognise the branch; prove containment by tree equality against the squash commit before deleting (the same rule as the identity-transfer rule in docs/verification.md; the controller's own v3.0.4 cleanup hit exactly this).

Post-fix state (measured): compute_status clean (0/0; 29 up-to-date, 3 project-custom); manifest 32 tracked / 0 missing / 0 localHash mismatch; key order PASS; lastSynced 1450034 = v3.0.4^{commit}; nine installed hooks/libs byte-identical to the tag; gate 901c5f5 674 passed, 98.57 %.

# Addenda 5 — warm-up re-measurements and the BOM reclassification (after docs/v31-specs 3af8048)

## open-brain — GREEN on `v3/v31-warmup` @ c079fcd (independent fixture, private extraction, read-only; both trees asserted non-repos before measuring; no 127)
ARM A red control (unfixed ps1 @ v3.0.4, no-.git): exit 1, 30 files, manifest ABSENT. ARM B fixed ps1 @ c079fcd: exit 0, 31, present. ARM C sh control: exit 0, 31, present. Parity 31 == 31; fixed writes more than unfixed. **Unrequested ARM D (happy path):** WITH-.git fixture at a known commit — exit 0 and `lastSynced` = the real short sha (aeae729), NOT the `unknown` fallback → the save/restore guard does not swallow the success path. Degraded-path parity: both scripts record `lastSynced: "unknown"` on no-.git. Auto-mode snippet: fixed run prints the reference entry 1×, unfixed 0× — both halves of the claim measured separately.

## BOM RECLASSIFIED — not writer hygiene: a MID-SYNC CRASH in the skill's own step 7b on the ps1 bootstrap path
Measured: ps1 manifests carry BOM=True AND CRLF=True (with or without .git); sh manifests neither. The MCP server TOLERATES it (`template_load_manifest` → valid, no errors/warnings; `localHash` normalises newlines and strips the BOM, so templateHash values are identical across both writers — divergence is in the FILE bytes only). What does NOT tolerate it: SKILL.md step 7b instructs a scratchpad `json.load`/`json.dump` with "indent=2, LF, no BOM" — a WRITE instruction; nothing says READ with `utf-8-sig`. Plain `json.load(io.open(path, encoding="utf-8"))` on a ps1-written manifest raises `JSONDecodeError: Unexpected UTF-8 BOM (decode using utf-8-sig)` — measured (open-brain's ARM D read threw it first; their own morning 7b script would have died the same way and survived only because open-brain was sh-bootstrapped). **Consequence: every ps1-bootstrapped consumer's FIRST `/sync-template` crashes at 7b, after finalize has written the manifest, at the step whose job is making the version legible. Loud, mid-sync, no clue why, and only on the path nobody on POSIX reproduces.**
*Decisions:* (1) SKILL.md 7b (and every step that parses the manifest) reads with `utf-8-sig` — one word; by the patch rule (security bypass / gate integrity only) it ships in v3.1 Phase 4, NOT as v3.0.5 — none of the five consumers is ps1-bootstrapped (all synced past 7b), so no live consumer is exposed today; if a ps1-bootstrapped consumer appears before v3.1, the one-word change is the first thing they are told. (2) Plan Phase 1 Task 1.5 rationale upgraded: "not cosmetic — a ps1-written manifest crashes the skill's 7b read; the server tolerates the BOM, the skill does not". (3) Skill's 2b/7b/step-6 python snippets all open manifests with `utf-8-sig`.

# Addenda 6 — panoscribe's warm-up re-measurement (c079fcd)

**GREEN on all four arms, protected main, PAIRED HARNESS:** two scratch harnesses byte-identical except `hooks/lib/git-cmd.sh` (old a37e2891f5f9 == v3.0.4 tag; new 222e95f3ad7d), payload cwd = the real panoscribe on protected main; fixture asserted first (harnesses differ, gate-before-merge.sh identical, both libs parse). Both libs through the same probe is what makes a want-0 GREEN evidence — the old column proves the harness can see the block.
```
arm  want  old  new
A    0     2    0    git -C <abs> merge-base --is-ancestor a b
B    0     0    0    git merge-base --is-ancestor a b  (no -C)
C    2     2    2    git -C <abs> merge feature/x       (positive control)
D    0     2    0    git -C <abs> merge-tree a b
R1–R4 (merge --no-ff / gh pr merge --squash / merge --squash / bare merge): 2 → 2 — fix costs no coverage
```
**BONUS ROW N2 (not in A6.14): `git -C <abs> config --get merge.tool` old=2, new=0.** A pure READ over-blocked because the config key NAME contains the verb — same class, different SHAPE (option VALUE, not a hyphenated subcommand); a fixture keyed only on `merge-*` subcommands would miss it; fixed for free by the positional walk. *Decision:* NOT added to the warm-up tip (it would move c079fcd and restart the ~90-min matrix for regression coverage of a case already measured GREEN); added as a row in Phase 2's suite change (Task 2.6b touches `scripts/test-hooks.sh` and runs the matrix anyway). Record as "measured GREEN on c079fcd by panoscribe; row pending in Phase 2".
**ANOMALY N3 (reported, not explained):** `git -C <abs> show-branch --merge-base a b` was ALREADY 0 under the OLD lib, although `\bmerge\b` matches inside `--merge-base` (non-word chars both sides) — something downstream of the fallback match cleared it; N1 `log --merges` = 0 is expected (`s` is a word char). So "every `\bmerge\b` match blocked" was FALSE for the old lib; a fixture written on that assumption would have a row passing for the wrong reason. *Open item for the lib reading / Phase 2:* find the downstream condition (probably the option-vs-subcommand classification of `--merge-base` as a flag token) and state it in the A6.14 comment so the rows' rationale is exact.
## penumbra — GREEN on c079fcd (paired against the installed old lib; fixture repo with **Gate**/**Protected branches** asserted; payloads from a script file, JSON-validated; from main AND f/x)
Rows 1/2/3/6 old 2 → new 0 (want 0); rows 4/5 controls 2 → 2; rows 8/9 (`git -C $T merge f/x`, `git -C $T push origin main`, literal `$T`) 2 → 2 with the class-3 refusal text on both libs — class-3 INTACT on gated verbs. **CORRECTION to the controller's note ("class-3 deliberately unchanged, still fires"):** row 7 `git -C $T merge-base HEAD HEAD~1` → old 2 (class-3 text), NEW 0. Root cause from the diff: the token walk no longer classifies `merge-base` as `merge`, so the segment never enters the merge arm, and the class-3 check INSIDE that arm is never reached. That is exactly fold-in §3.1 item 1's design ("resolve the subcommand first; refuse an unresolvable `-C` only for gated verbs") — GREEN by the spec — but (a) the PR body / CHANGELOG must NAME it as a behaviour change or the next reader re-files it as a regression, and (b) A6.14 needs row 7 as a want-0 row with its own DtG (old fallback restored → 2). *Decision:* row 7 (with N2) goes into Phase 2's suite change (adding it now moves the tip and restarts the matrix); PR #88 body states the change now. *Asymmetry note:* `pre-commit-test.sh`'s class-3 check precedes verb classification (yutraffic's `git -C "$VAR" cat-file` was refused by THAT hook) → still 2 there; the two gates differ, as the fold-in's `-c`/class-3 asymmetry items already record; unifying is the v3.1 class-3 item. Feature-branch note: row 4 is 0 from f/x on both libs (merge-target logic keys on the checkout branch).

**Controller source read (v3.0.4, 2026-09-05):** (a) `2>&1`-as-mover MECHANISM confirmed — gate-before-merge.sh's preceding-clause classifier (`a6cc_*`, :462-465) tests `case "$a6cc_seg" in *'>'*|*'<'*) A6_CLASS=mover` — a CHARACTER test ("carries a redirection operand, which can write .git/config"), so `2>&1`, `>&2`, and any `<`/`>` inside a quoted argument classify as movers; Task 2.6b replaces the character test with a redirection-operand parse (`>`, `>>`, `&>`, `>|`, `<` followed by a file token; fd duplications `[n]>&m` inert). Also seen: the inert first-word list is `echo|printf|ls|pwd|true|:` and the read-only git verb list for preceding clauses is `status rev-parse branch log diff show` (with `a6_inert_flags`), which is why `git fetch` before a pull was refused as a mover today. (b) N3 NOT resolved by reading: before the merge arm (:824) only `gc_cd_target` and `a6_branch_move` (checkout/switch via gc_matches_subcommand) run; neither matches `show-branch`; the old fallback `\bgit\b.*\bmerge\b` DOES match `--merge-base`. So the old-lib 0 needs a RUN against v3.0.4 with panoscribe's exact payload (possibly `a6_deny_unresolved_c` or `gc_repo_for` on their `<abs>` operand, or the segment split) — open item, Phase 2, before the A6.14 comment claims a mechanism.
**Runtime:** ~10 s per gate-before-merge.sh invocation on this Windows host (git subprocesses); 28 invocations exceeded a 300 s budget. Note for the suite: A6.14 added 8 rows to a 972-row suite; a Windows wall-clock line in docs/verification.md so a timeout is not read as a hang.
panoscribe will run the five `2>&1` arms from the same harness when Task 2.6b has a candidate.

# Addenda 7 — yutraffic's warm-up re-measurement (c079fcd) — fourth of four GREEN

Baseline installed v3.0.4 (lib a37e2891f5f9) vs warm-up lib 222e95f3ad7d; the three gate scripts byte-identical between them, so every delta is the lib alone.

**Controller corrected twice:** `git -C "$VAR" cat-file -p HEAD:VERSION` is **0 through all three hooks on BOTH libs** (pre-commit-test 0/0, gate-before-merge 0/0, no-push-main 0/0). It never refused on v3.0.4; "still 2" was wrong, and the correction "through pre-commit-test still 2" was wrong for this row. Which hook originally refused it: **none**. The fold-in's record of the row as → 0 is the accurate one. **Why (measured):** pre-commit-test's class-3 refusal fires only on a row carrying ITS OWN gated verb — `git -C "$VAR" commit -m x` → 2 on both libs with the class-3 text; `cat-file` carries no commit segment, so the hook exits before class-3 is consulted. The ORDER statement (class-3 before classification inside pre-commit-test) is right; which rows can observe it was wrong.
**Nothing widened:** every row that refused on v3.0.4 still refuses on c079fcd (`git -C "$VAR" merge f/x` 2/2; `git push origin main` 2/2; `git -C "$VAR" push origin main` 2/2; `git -C "$VAR" commit -m x` 2/2). The single behaviour change: `git -C "$VAR" merge-base HEAD HEAD` on gate-before-merge 2 → 0 — penumbra's row exactly.
**New rows all 0 on the warm-up lib:** `-C <literal> merge-base`, `log --grep=merge`, `log -- docs/merge.md`, `-C <literal> cat-file`; controls sound (`merge feature` 2, `push origin main` 2, `status` 0, `commit -m x` 0).
**Retraction (theirs):** "TIMEOUT on v3.0.4 → 2/0 on warm-up" for two rows was a 20 s subprocess ceiling under load, not behaviour; re-timed at 90 s both libs give 2 and the hooks run in 1–3 s warm. Their earlier "5–16 s per hook" was cold-start noise — not a profiling point. (panoscribe's ~10 s/invocation figure stands as their measurement; the docs line says "cold start; warm 1–3 s".)
**FIXTURE FINDING → Phase 2 suite row (Task 2.4 / 2.8):** hooks extracted WITHOUT `run-gate.sh` beside them make pre-commit-test exit 0 on a commit row with only "WARN: run-gate.sh not found next to this hook", minting no artifact — a want-0 commit row in that state passes while proving nothing (the vacuous-fixture shape). Discriminator to assert in test-hooks.sh: delete `.gate/last-pass.json`, run the commit row, require the artifact to come BACK (`status: pass`); with run-gate.sh present their fixture minted `status=pass tree=50eb222efc60` and printed "PRE-COMMIT: Running 'run-gate.sh'...". That is what makes every 0 load-bearing rather than "nothing to gate".

# Addenda 8 — penumbra's "MCP merge path ungated" report (2026-09-05) — WITHDRAWN ON MEASUREMENT
**Result:** (1) penumbra's `.claude/settings.json` registers `gate-before-merge.sh` on `mcp__MCP_DOCKER__merge_pull_request|mcp__github-tools__github_pr_auto_merge` exactly as the template does (the premise "not on its matcher" was an assumed list, never read). (2) Probe from protected main (HEAD 6b013ea) with the CURRENT stale artifact (sha df3fd10, two merges old): `mcp__MCP_DOCKER__merge_pull_request` payload → 2 "BLOCKED: gate-before-merge refuses this operation on a protected branch"; `mcp__github-tools__github_pr_auto_merge` → 2 same; control `gh pr merge 153 --squash --delete-branch` → 2 same; `ls -la` → 0. The coder's merge went through with the gate RUNNING and PASSING silently on a fresh tree-matching artifact. **Not a gate-integrity defect; not a v3.0.5 trigger.** Residual → skill sentence (Phase 4): "a permission denial on `gh pr merge` sends an agent to the MCP merge tool; that is fine — the same hook is registered on that matcher and runs the same artifact check". Original report below for the record.

Report: a python-coder on `docs/retro-audit-batch3` ran `bash hooks/run-gate.sh` (GATE PASS, tree == HEAD), was DENIED by the auto-mode classifier on `gh pr merge 153 --squash --delete-branch` (permission, not a hook), then merged via `mcp__MCP_DOCKER__merge_pull_request` (dbb5789 → 6b013ea). penumbra's claim: gate-before-merge.sh is on `Bash|PowerShell` only, so no artifact check ran on the MCP path; CLAUDE.local.md points consumers at the MCP tool as the preferred door.

**Controller measurement (2026-09-05):** `templates/*/.claude/settings.json` registers gate-before-merge.sh on `mcp__MCP_DOCKER__merge_pull_request|mcp__github-tools__github_pr_auto_merge` in ALL SIX variants (grep -c = 1 each) and the toolkit root does too; `user-level-reference/settings.json` does not (user-level has no project hooks — correct). The hook exits 0 SILENTLY on a fresh matching artifact — penumbra's exact state — so the observed outcome is consistent with the gate RUNNING and PASSING. The claim is an inference from an assumed matcher list. Asked penumbra for two measurements: (1) `grep -c merge_pull_request .claude/settings.json` on their tree; (2) probe the MCP path with a stale/missing artifact → expect 2. **If either shows the gate absent → gate-integrity defect → v3.0.5 trigger under the patch rule.** If both hold → covered; skill gets one sentence: "a permission denial on the gh path is not a reason to reroute; the MCP path is gated identically".
Consumer-side notes recorded: `VAR=…; mv …; git pull --ff-only` refused by the mover rule naming the assignment (correct; two-call form passed); untracked copies of files the merge makes tracked block the fast-forward ("would be overwritten by merge") — consumer trap, not a hook matter.

# Addenda 9 — MM-Agent synced v3.0.4 (2026-09-05): ALL FIVE consumers now on v3.0.4

Committed 1f526d7 on `m113-session-2026-09-03`, pushed. 2 auto-updates (run-gate doc note, deny-secret-reads message), 1 conflict (PROJECT_CONTEXT.md `&&`-never-`;` comment — spliced by hand, keep-mine registered), 2 TEMPLATE_DELETED (the acknowledged pair, deferred again — `deletedAcknowledged` still unconsumed on 0.3.0, as expected until the skill passes `deleted_files`). Post-stamp: 0/0, 29 up-to-date, 3 project-custom; manifest 1450034 / v3.0.4 / 1450034. **Wall clock ~25 min incl. one 10-min pre-commit Test run — the v3.1 baseline for this consumer on a docs-only release** (vs 2–3 h estimated for a hook release with full verification).
- Step 1 marker: body v3.0.4 == installed v3.0.4 (the restart made it true; the previous session still had the v3.0.3 body loaded — the marker assertion had stopped it, as designed).
- **§1b case 2 fired (fifth of five consumers):** checkout 5d7bbb1 = `v3.0.4-1-g5d7bbb1`; tracked diff clean → recorded 1450034. Finalize wrote 5d7bbb1 (records HEAD unconditionally — confirms the v2-path statement). *Skill item:* the 7b snippet must carry the case-2 branch EXPLICITLY (their stamp script's `assert lastSynced == version_of` from v3.0.3 had to become an ASSIGNMENT); the text's "(or have finalize record it)" arm is dead on the v2 path (already marked; the observable `template_commit` rule covers v3). *Report shape:* after the rewrite compute_status shows `template_commit 5d7bbb1` vs `last_synced_commit 1450034` with everything UP_TO_DATE — correct, but reads as "not synced to HEAD" to someone comparing shas; the report's Toolkit line is what makes it legible — say so beside the rewrite.
- 2b: three sets; (a) CLAUDE.local.md, (b) empty (structural), (c) 11 agents incl. the 2 project agents; control passed both arms. Live-run rule honoured (no game, no agents): both hook writes landed first try, no WinError 5 — the discriminating fact stands.
- deny-secret-reads v3.0.4 probed from a script file on the project copy: Read `.env`/`.env.local` → 2 with "(matched: … in the file path argument)"; `cat .env` → 2 with "argument N" + whole-command explanation; README/`cat README.md` → 0. 6b 13/13/0 + libs; probe 2/0/0 node; 6d 0→0 across 10 probes; sweep 1/1/1; drift 28/28.
- **Finding for the ownership model (their #10):** AGENT_TEAM.md had drifted to PROJECT_CUSTOM because a project rule added yesterday landed OUTSIDE its PROJECT-CUSTOM region (template section "Worktrees / Rules"). Under v3.1 AGENT_TEAM.md is template class → LOCAL_EDITED → overwritten with the diff saved. They moved the rule into the region (region_only again, UP_TO_DATE). *Spec/skill item (§7 LOCAL_EDITED report + Phase 4):* when a LOCAL_EDITED template-class deviation is a PURE INSERTION, the report names the remedy — under v3.1 "move lines X–Y to `.claude/rules/project.md` (project-owned); the next sync is then clean" — not only "diff saved to the backup dir"; a backup makes the next sync the same fight. (Under v3.1 AGENT_TEAM.md has no region either; project.md is the only home.) Server side: `local_diff` already carries the hunks; classifying "pure insertion" (only `+` lines) is one check — **Server batch 10 ACCEPTED:** `template_compute_status` (v3) adds `local_diff_kind: "insertion" | "mixed"` beside `local_diff` for every LOCAL_EDITED template-class file (insertion = diff body only `+` lines); lands as a 0.3.1 candidate (branch + PR now; cutting 0.3.1 is their user's call). The skill gates the "move lines X–Y to `.claude/rules/project.md`" remedy on `local_diff_kind == "insertion"`; the toolkit's v3.1 `requires_server` floor stays `>=0.3.0` — the remedy sentence degrades to the generic one when the field is absent.
