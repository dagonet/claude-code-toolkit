# v2.1 — align with Boris Cherny's 2026 guidance

**Tier:** T4 (workflow change; 3 sequential PRs)
**Team:** architect, coder, code-reviewer, tester
**Gate:** `bash scripts/verify-template-consistency.sh && bash scripts/test-hooks.sh`
Challenge 1 / Challenge 2: to be run by the architect before PR7 starts (this file is the target).

## Context

v2.0 (PRs #52–#57, tag `v2.0`) aligned the toolkit with Anthropic's docs. A follow-up search on 2026-08-29 reached Boris Cherny's own 2026 statements (Jan 2 setup thread; Jun 8 "Reflecting on a year of Claude Code" with Cat Wu; Jun 9 Fable 5 + nested subagents; Jul 24 "new rules of context engineering"; Jul 2026 Opus 5 conversation — "we deleted 80% of the system prompt"). Most of it is already reflected in v2.0 (context minimalism −37%, write-it-down → retro ledger + rules, subagents + `isolation: worktree`, PostToolUse hooks, auto mode). Four points conflict with what v2.0 kept; the user decided each on 2026-08-29:

| Boris (2026) | v2.0 today | Decision |
|---|---|---|
| "I don't use plan mode anymore… starting with 4.6, definitely 4.7, it just doesn't need it." Full task context upfront instead. | `EnterPlanMode` mandated for T2+; `hooks/tier-before-coder.sh` blocks coder spawns without a Tier/Challenge-1/Challenge-2 plan file | **Drop the plan gate entirely.** Plans become optional artifacts. |
| `xhigh` is the new default reasoning level (Opus 4.7+); Fable decides per step from effort | `effortLevel: medium` documented as session default; reviewers `effort: high` | **Unset the session default (model default); architect/code-reviewer `effort: xhigh`.** |
| Fable 5: "best model I have used for coding, by a wide margin… higher trust & autonomy" (2× Opus price) | opus/fable listed as equals | **Fable for T3/T4 orchestration, Opus for T1/T2** (documented; user picks per session). |
| "use a workflow" for tasks too big for one pass; routines for recurring maintenance | 0 Workflow uses in 6 weeks; no routines | **T4 → "use a workflow"; ship a nightly retro/drift routine template.** |

Sources indexed in the context-mode KB: `boris-cherny-opus5-80pct-system-prompt`, `boris-cherny-tips-aggregator` (secondary aggregator of primary X posts; each tip links its post).

## Challenge record (architect two-pass, 2026-08-29)
Accepted: no `## Task brief` hook extension (prose contract in AGENT_TEAM.md instead; `require-skills-block.sh` unchanged); stub removals move PR7 → PR9; the six v2.0 minors leave PR9 (own PR later); no tier fixtures exist in test-hooks.sh; hook-ref invariant + mirror check 21 are derived (no edits) — the real assertion edits are check 6 (`Plan Challenge Protocol` presence → rewrite to assert absence), `:331-341` (+`:369`) teammate-stub asserts, `test-hooks.sh:239-243` block-bash-vcs stub fixture; settings.json edit-general-then-cp ×5; plan-mode surfaces list (AGENT_TEAM.md:70,94-95,109-115,495-516,573-604; CLAUDE.md:8,13; karpathy SKILL.md:88; user CLAUDE.md:62; settings-reference.md:247,339-342; docs/templates.md:19; docs/workflow-audit.md:18,55; check-activation.sh:66); README hard-coded counts (`:23`, `:48`); `effortLevel` stripped from settings.json **and** settings-reference.md:106,210,437,441 in lockstep; PR8 gated on a live spawn proving `effort: xhigh` is accepted (fallback `high`); migration adds `rm ~/.claude/hooks/{tier-before-coder,block-bash-vcs,require-teammate-report}.sh` and consumer `hooks/` deletions via `/sync-template`.
Kept against advice: the routine template (user decision 12) — format verified against the `schedule` skill/docs before PR9; fallback = skill with `disable-model-invocation: true`.

## PR7 — drop the plan gate; full task context upfront
- `hooks/tier-before-coder.sh` → delete (and `user-level-reference/hooks/tier-before-coder.sh`); remove the PreToolUse `Agent` → tier-before-coder entry from `templates/*/.claude/settings.json` ×6 and `user-level-reference/settings.json`; keep `require-skills-block.sh` (spawn contract) and `enforce-delegation.sh` (PO never hands-on — unchanged decision).
- `templates/general/AGENT_TEAM.md` (cp ×5): tiers stay as **size caps** for team composition; delete the `EnterPlanMode` mandate and the "Plan Challenge Protocol" as a gate; replace with **"Task brief upfront"** — every spawn prompt carries goal, constraints, acceptance criteria, files in scope (the `## Required Skills` block stays). Plan files in `docs/plans/` become optional ("write one when the work spans sessions or needs a decision record"); `/challenge` stays available as an optional skill. Remove the `Team:`/`Tier:` plan-file literal requirements from docs.
- `templates/*/CLAUDE.md` Workflow TL;DR step "Enter plan mode for T2+" → "Write the task brief; spawn". `user-level-reference/CLAUDE.md`: no plan-mode wording.
- `scripts/verify-template-consistency.sh`: remove/rewrite assertions that pin tier-before-coder (hook-ref invariant, mirror check, settings matcher); keep counts derived. `scripts/test-hooks.sh`: drop tier-before-coder fixtures.
- `scripts/check-activation.sh`, `docs/*`, README hook tables, `AGENTS.md`: sweep `tier-before-coder|EnterPlanMode|Plan Challenge|Challenge 1|Challenge 2|plan file`.
- Also remove the two v2.0 no-op stubs promised for v2.1: `hooks/block-bash-vcs.sh`, `hooks/require-teammate-report.sh` (+ their assertions).
- Migration note: delete the Agent-matcher entry from consumer `settings.json` if locally modified; `~/.claude/settings.json` loses its tier-before-coder hook.

## PR8 — effort + orchestrator policy
- `user-level-reference/settings.json`: remove `effortLevel` (model default); keep `advisorModel: opus`. `settings-reference.md`: explain "unset = model default; Fable 5 ignores thinking toggles; raise per role via agent `effort:`".
- Agents ×7 sets: `architect.md`, `code-reviewer.md` → `effort: xhigh`; coders stay `medium`; Explore `low`. Verify check 18/R2 expectations updated (derived).
- `AGENT_TEAM.md` `## Model & Effort Policy`: orchestrator = **Fable 5 for T3/T4 sessions, Opus 5 for T1/T2** (user picks with `/model`; cost note 2×); workers `sonnet`; Explore `haiku`; diagnostic rule kept; proxy warning kept.
- `templates/*/CLAUDE.md` Session Bootstrap: one line "T3/T4 session → `/model fable`".

## PR9 — dynamic workflows + routine template + release v2.1
- `AGENT_TEAM.md` T4 row: "for work too big for one pass say **use a workflow** — orchestrator fans out implementer → two verifiers → fixer per task; default to auto mode so it never stops for permissions; sequential/same-file work stays a single session". `skills/sprint/SKILL.md`: point T4 sprints at workflows; keep the merge/gate rules.
- New `user-level-reference/routines/nightly-retro-drift.md`: a routine prompt (for `/schedule`, cron `0 6 * * *`) that runs `bash hooks/retro-brief.sh`, `bash scripts/verify-user-level-drift.sh`, and the consumer-repo `/sync-template` status, and files one summary (issue or `docs/retro-summary.md`). Document in `user-level-reference/README.md` + `docs/verification.md`. (Verify the routine file format against `/schedule` docs first; if routines need a different shape, ship the prompt as a skill `routine-nightly-retro` with `disable-model-invocation: true`.)
- `VERSION` → 2.1; CHANGELOG `## v2.1` roll-up (+ `### Downstream migration`, `### Removed`: the two stubs + tier-before-coder). Cheap v2.0 minors folded in: `tools/measure-context-bloat.py` UTF-8 (`PYTHONIOENCODING`), `verify-user-level-drift.sh --expect-drift`, `skills/commit/SKILL.md` jargon, near-empty `## Working Preferences` heading, duplicated formatter lines in rules, `_note_git_tools` key. Remaining minors stay listed.

## Verification
Per PR: `verify-template-consistency.sh` green (assertions updated deliberately, listed); `test-hooks.sh` green; byte-identity md5 ×6; `grep -rnE 'tier-before-coder|EnterPlanMode|Plan Challenge|Challenge [12]|effortLevel' templates user-level-reference docs README.md AGENTS.md` → 0 outside CHANGELOG/docs/plans; `setup-project.sh` dry-run on one variant; `/sync-template` status in one consumer repo. Live smoke after PR7: spawn a coder without any plan file → not blocked; `## Required Skills` still enforced.

## Risks
- PR7 removes the only artifact-based gate before coding; mitigations are the brief contract in `require-skills-block.sh` (extend it to require a `## Task brief` block with goal/constraints/acceptance criteria — one fixture) and the review/tester pipeline, which is unchanged.
- Fable 2× cost — documented, not defaulted.
- Routine file format unverified — fallback to a skill.
