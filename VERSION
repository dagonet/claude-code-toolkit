2.1.1
Built from three consumers' /sync-template reports: the git gates now fail closed when their sourced lib is missing or a pre-v2 MCP matcher reaches them, the git-tools MCP write ops are denied so the native-git gates cannot be bypassed, and the SubagentStop matchers match any <lang>-coder instead of the template's own list. 12 hook scripts, 8 skills; always-loaded config 25,999 B, down 36.8% from the 41,167 B baseline; 192 consistency assertions and 156 hook fixtures.
