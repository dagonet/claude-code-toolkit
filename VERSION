1.2
Liveness caps corrected — the TeammateIdle gate no longer exhausts itself mid-session (3 blocks to 41 on real data), the tool-call budget escalates instead of blocking once, and both hooks leave an audit trail.
