3.0.4
A patch of the cheap items five consumer sessions measured in their first v3.0.3 sync, none of them new design, and the v3.1 deferrals named rather than left silent: misplacement-aware three-way merge, lastSyncedVersion ownership, tracked-only tree hashing, finding-59 provenance, profiling, class-3 refusal on read-only verbs, the -C fallback that classifies merge-base as a merge, and setup-project.ps1's unguarded git rev-parse terminating before the manifest is written on a no-.git checkout. deny-secret-reads.sh's DENY text now names the matched token and the argument it sat in, states the rule the hook actually applies — a listed reader verb together with a secrets-file token anywhere in the whole command, across clauses, deliberately, so a bash -c wrapper cannot hide the read — and points at the Read tool for inspecting a config file that merely mentions one; the first draft's "matched the command TEXT" clause was measured false and never shipped, and a draft remedy that named a script file was rejected as an evasion recipe against the very guard it explained. run-gate.sh's Gate extractor gained eight permanent marker-fixture rows, with `bash real-gate.sh ; true` kept as the over-correction control that goes red only if honest values start being refused; the fifth extractor is measured through a marker in the Gate value with pre-commit-test.sh byte-unchanged; the tree arm's untracked-file sensitivity is documented rather than fixed; every PROJECT_CONTEXT.md says join Gate steps with && never ; because ; mints a pass on a failing suite; check 21 walks both directions with a four-entry HOOKS_NO_MIRROR allowlist and retro-brief.sh is mirrored as the reading half of the retro pair; and check 34 asserts heading-anchored placement against a real, sanitized three-way fixture that is red on the misplaced merge and green on the correct splice. The sync skill refuses an untagged toolkit checkout, records the TAG commit when a docs-only descendant's tracked tree is byte-identical, plants its 2b control with a leading newline and asserts the last real .gitignore rule survived, defines merge correctness as placement AND presence, compares tracked-ness against the manifest plus this sync's applied set, generalises the /tmp warning to every bash-to-python handoff, keeps 7b because template_finalize_sync never writes lastSyncedVersion, documents that hook-file writes fail with WinError 5 while a live run holds the broad-matcher hook open, adds a fourth presync trap for fixtures pushed without -u, and qualifies its bootstrap claim for the no-.git ZIP path. Gate 964/0/0 with skipped == 0 reported explicitly; parser matrix passed in all three configurations; five consumer verdicts green on the final tip, each transferred by verified identity or re-read where the file is executed procedure.
