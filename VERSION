2.1.5
Consumer feedback from Yutraffic-Challenge (PR #223) and panoscribe (PR #123): hooks/run-gate.sh now keys its artifact on the WORKING TREE via a temp-index add -A hash instead of the index (the PreToolUse hook fires before a chained git add && git commit stages anything, so the old key matched nothing and the single-run merge path never fired for agents) — git add -A && git commit, git commit -a, and separate add/commit calls all yield HEAD^{tree} == tree, while a partial-add commit mismatches by design; hooks/enforce-delegation.sh now evaluates the Bash command per segment and exempts segments whose first token is git or gh, so git add hooks/run-gate.sh and a commit message naming a test runner no longer block the PO's own sync commit (git add x && bash hooks/run-gate.sh still denies, and a VAR=value strip closes the CI=1 pytest hole); the sync-template skill gained the full commit-then-gate-then-PR-then-merge sequence, a positive control proving the gates are live rather than merely unblocked, and a heading-anchored context-mode grep; AGENT_TEAM.md and the 11 coder contracts note that CI produces no run for a bare branch push and that the gate keys on the working tree. 12 hook scripts, 8 skills; 212 consistency assertions and 202 hook fixtures.
