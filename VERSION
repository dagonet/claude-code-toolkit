3.1.2
template_version is derived from VERSION instead of a literal that survived the last bump and stamped projects with a tag their own template_commit contradicted, and the sync skill's header no longer names a floor three minor versions below the one its first step enforces.
