2.1.4
Follow-ups from penumbra's third sync report and two deferred review minors: the v2.1.3 CHANGELOG's second-gate-run framing is corrected (a Test-field repo — five of six templates — still runs run-gate.sh separately before merge; the tree-keyed artifact only spares a re-run on a later tree-identical commit, or lets a Gate-only repo's commit-time run count); the sync skill's smoke-test guidance now warns that an inline hook probe mentioning a guarded command trips the gate it is testing, and step 9 stages exactly the sync's touched files instead of git add -A; hooks/retro-ledger.sh tallies an agent-budget-warn.sh block separately as budget=<n> instead of folding it into blocks=[...], since a budget ceiling is an expected liveness control, not a failure; a dead placeholder guard in pre-commit-test.sh was removed as unreachable; and the hook-name extraction regex in verify-template-consistency.sh's check 13 was widened to accept digits/underscores. 12 hook scripts, 8 skills; 212 consistency assertions and 184 hook fixtures.
