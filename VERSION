2.2.3
Warning is not protecting. v2.2.1 made an unreplaced `{{DEFAULT_BRANCH}}` fall back to `main master` and warn — but `main master` does not contain `develop`, so a develop-trunk repo carrying the placeholder was left exactly where v2.2.0 left it: its trunk pushable, with a `json_warn_once` line on stderr that reaches nobody's transcript. Found by executing the v2.2.2 path against real repos, not by reading the fixtures. `gc_protected_branches`' placeholder arm now resolves the repo's real trunk from `git symbolic-ref --short refs/remotes/origin/HEAD` and UNIONs it with `main master` — union, never replacement, so a repo with both does not lose main's protection to a fix for develop's; deduped; and explicitly not "every local branch", which is the mirror of the original bug. The resolution went into the hook resolver rather than the apply layer because the apply layer is not in this repository (`template_apply_file` is upstream), because the exposure is live now and one file copy repairs every already-synced repo, and because the git call sits INSIDE the placeholder arm, so a configured repo never makes it and the command is a local ref read that cannot hang. The upstream proposal's current-branch fallback was deliberately dropped in the hook: at push time HEAD is the feature branch being pushed, and protecting it would block the developer's own push — `setup-project.sh` keeps that fallback because at bootstrap the checkout IS the trunk. Fail-safe unchanged: no remote, unreadable `origin/HEAD`, a non-ref-shaped value or a placeholder value all yield `main master`; never empty, never the literal. Absent and empty arms untouched, deliberately. The `- **Branch strategy**:` prose line drops `{{DEFAULT_BRANCH}}` and points at `**Protected branches**:` instead — no manifest fills that key, so the queued `unresolved_placeholders` check would have warned on every apply for everyone forever on a placeholder that belongs there, and an exception list would have made the warning correct by making it blind. `sync-template` step 7 gains one sentence: re-register keep-mine files as the last action before finalize, because a hash recorded before a later edit describes nothing. Downstream: re-copy `hooks/lib/git-cmd.sh` (+ its `user-level-reference/` mirror) and `skills/sync-template/SKILL.md`. 12 hook scripts + `hooks/lib/`, 8 skills; 261 consistency assertions, 327 hook fixtures.
