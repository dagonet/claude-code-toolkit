3.1.4
Step 1c named two different markers as the value to pass and the wrong one was easier to reach for, so a caller who had done everything right could be refused; and check 41 pins the deprecated-key tolerance that check 40 left without a template-side user.
