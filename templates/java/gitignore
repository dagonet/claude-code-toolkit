# Claude Code - machine-specific files (not committed)
# .claude/settings.json and .claude/agents/ ARE committed (shared with team)
CLAUDE.local.md
.claude/.mcp.json
# project-scope MCP servers (machine-specific paths)
/.mcp.json
.claude/liveness.log
.gate/
# agent worktrees — the WORKTREE_BASE default in setup-project.sh/.ps1
.claude/worktrees/
