# Claude Code - machine-specific files (not committed)
# .claude/settings.json and .claude/agents/ ARE committed (shared with team)
CLAUDE.local.md
.claude/.mcp.json
# project-scope MCP servers (machine-specific paths)
/.mcp.json
.claude/liveness.log
.gate/
# EnterWorktree's fixed location (no setting); the bootstrapper's default now lives OUTSIDE the repo (../.worktrees/<project>) and needs no ignore
.claude/worktrees/
