4.1.2
Rollout patch: one command-origin backslash-continuation join (cmd_join_continuations, hooks/lib/json.sh) closes the wrapped-command-add, gh pr merge and cat .env fail-opens across every git gate and deny-secret-reads.sh; the seed's harm-keyed arm keys on the guidance sentence rather than the PROJECT-CUSTOM token; the gate fingerprint's pyvenv component becomes interpreter-prefix identity so a no-venv repo can earn the tree+env extension; the bootstrap worktree default moves outside the repo, anchored to the resolved toplevel; the consistency script gains a newest-first CHANGELOG check and a skill-doc-vs-code LINES check; and the sync-template skill plus AGENT_TEAM.md gain the hook-probe rule, the rc=$? capture, the orphan-job rules, and the declared worktree location.
